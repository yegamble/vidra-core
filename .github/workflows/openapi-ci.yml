name: OpenAPI CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  validate-and-test:
    name: Validate OpenAPI and Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: athena_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.6'
          cache: true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Swagger CLI
        run: npm install -g @apidevtools/swagger-cli

      - name: Validate OpenAPI spec (Makefile)
        run: make validate-openapi

      - name: Apply test migrations
        env:
          DATABASE_URL: postgres://test_user:test_password@localhost:5432/athena_test?sslmode=disable
        run: |
          echo "Applying test migrations for OpenAPI CI..."
          make migrate-custom

      - name: Install dependencies
        env:
          GOPROXY: https://proxy.golang.org,direct
          GOSUMDB: sum.golang.org
          GOPROXY_TIMEOUT: 600s
        run: |
          # Configure Go timeouts and retries for network issues
          for i in {1..5}; do
            if timeout 600 go mod download; then
              echo "Dependencies downloaded successfully on attempt $i"
              break
            else
              echo "Download attempt $i failed, retrying in 30 seconds..."
              if [ $i -lt 5 ]; then sleep 30; fi
            fi
          done

      - name: Run tests
        env:
          # Standardized test environment
          DATABASE_URL: postgres://test_user:test_password@localhost:5432/athena_test?sslmode=disable
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: test-jwt-secret-for-ci
          IPFS_API: http://localhost:5001
          REQUIRE_IPFS: false
          VALIDATION_TEST_MODE: true
          LOG_LEVEL: info
        run: |
          go test -v ./...

      - name: Build application
        run: go build -o bin/athena-server ./cmd/server

  generate-docs:
    name: Generate API Documentation
    runs-on: ubuntu-latest
    needs: validate-and-test
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Redoc CLI
        run: npm install -g @redocly/cli

      - name: Create docs directory
        run: mkdir -p docs

      - name: Generate HTML documentation
        run: redocly build-docs api/openapi.yaml -o docs/api-docs.html

      - name: Upload documentation
        uses: actions/upload-artifact@v4
        with:
          name: api-documentation
          path: docs/

      # Skipping GitHub Pages setup and deployment as requested.
