// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: job_status.sql

package sqlcgen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const accountExportRecentFailures = `-- name: AccountExportRecentFailures :many
SELECT id, last_error AS error, attempts, updated_at
FROM account_exports
WHERE state = 'failed'
ORDER BY updated_at DESC
LIMIT $1
`

type AccountExportRecentFailuresRow struct {
	ID        uuid.UUID `json:"id"`
	Error     string    `json:"error"`
	Attempts  int32     `json:"attempts"`
	UpdatedAt time.Time `json:"updated_at"`
}

func (q *Queries) AccountExportRecentFailures(ctx context.Context, limit int32) ([]AccountExportRecentFailuresRow, error) {
	rows, err := q.db.Query(ctx, accountExportRecentFailures, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []AccountExportRecentFailuresRow
	for rows.Next() {
		var i AccountExportRecentFailuresRow
		if err := rows.Scan(
			&i.ID,
			&i.Error,
			&i.Attempts,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const accountExportStats = `-- name: AccountExportStats :one
SELECT
    count(*) FILTER (WHERE state = 'pending')::bigint AS pending,
    count(*) FILTER (WHERE state = 'running')::bigint AS running,
    count(*) FILTER (WHERE state = 'done')::bigint    AS done,
    count(*) FILTER (WHERE state = 'failed')::bigint  AS failed,
    COALESCE(EXTRACT(EPOCH FROM (now() - min(created_at) FILTER (WHERE state = 'pending')))::bigint, 0)::bigint AS oldest_pending_age_seconds
FROM account_exports
`

type AccountExportStatsRow struct {
	Pending                 int64 `json:"pending"`
	Running                 int64 `json:"running"`
	Done                    int64 `json:"done"`
	Failed                  int64 `json:"failed"`
	OldestPendingAgeSeconds int64 `json:"oldest_pending_age_seconds"`
}

func (q *Queries) AccountExportStats(ctx context.Context) (AccountExportStatsRow, error) {
	row := q.db.QueryRow(ctx, accountExportStats)
	var i AccountExportStatsRow
	err := row.Scan(
		&i.Pending,
		&i.Running,
		&i.Done,
		&i.Failed,
		&i.OldestPendingAgeSeconds,
	)
	return i, err
}

const captionJobStats = `-- name: CaptionJobStats :one
SELECT
    count(*) FILTER (WHERE state = 'pending')::bigint AS pending,
    count(*) FILTER (WHERE state = 'running')::bigint AS running,
    count(*) FILTER (WHERE state = 'done')::bigint    AS done,
    count(*) FILTER (WHERE state = 'failed')::bigint  AS failed,
    COALESCE(EXTRACT(EPOCH FROM (now() - min(created_at) FILTER (WHERE state = 'pending')))::bigint, 0)::bigint AS oldest_pending_age_seconds
FROM caption_jobs
`

type CaptionJobStatsRow struct {
	Pending                 int64 `json:"pending"`
	Running                 int64 `json:"running"`
	Done                    int64 `json:"done"`
	Failed                  int64 `json:"failed"`
	OldestPendingAgeSeconds int64 `json:"oldest_pending_age_seconds"`
}

func (q *Queries) CaptionJobStats(ctx context.Context) (CaptionJobStatsRow, error) {
	row := q.db.QueryRow(ctx, captionJobStats)
	var i CaptionJobStatsRow
	err := row.Scan(
		&i.Pending,
		&i.Running,
		&i.Done,
		&i.Failed,
		&i.OldestPendingAgeSeconds,
	)
	return i, err
}

const captionRecentFailures = `-- name: CaptionRecentFailures :many
SELECT id, error, attempts, updated_at
FROM caption_jobs
WHERE state = 'failed'
ORDER BY updated_at DESC
LIMIT $1
`

type CaptionRecentFailuresRow struct {
	ID        uuid.UUID `json:"id"`
	Error     string    `json:"error"`
	Attempts  int32     `json:"attempts"`
	UpdatedAt time.Time `json:"updated_at"`
}

func (q *Queries) CaptionRecentFailures(ctx context.Context, limit int32) ([]CaptionRecentFailuresRow, error) {
	rows, err := q.db.Query(ctx, captionRecentFailures, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CaptionRecentFailuresRow
	for rows.Next() {
		var i CaptionRecentFailuresRow
		if err := rows.Scan(
			&i.ID,
			&i.Error,
			&i.Attempts,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const federationDeliveryStats = `-- name: FederationDeliveryStats :one
SELECT
    count(*) FILTER (WHERE state = 'pending')::bigint   AS pending,
    count(*) FILTER (WHERE state = 'running')::bigint   AS running,
    count(*) FILTER (WHERE state = 'delivered')::bigint AS done,
    count(*) FILTER (WHERE state = 'failed')::bigint    AS failed,
    COALESCE(EXTRACT(EPOCH FROM (now() - min(created_at) FILTER (WHERE state = 'pending')))::bigint, 0)::bigint AS oldest_pending_age_seconds
FROM federation_deliveries
`

type FederationDeliveryStatsRow struct {
	Pending                 int64 `json:"pending"`
	Running                 int64 `json:"running"`
	Done                    int64 `json:"done"`
	Failed                  int64 `json:"failed"`
	OldestPendingAgeSeconds int64 `json:"oldest_pending_age_seconds"`
}

func (q *Queries) FederationDeliveryStats(ctx context.Context) (FederationDeliveryStatsRow, error) {
	row := q.db.QueryRow(ctx, federationDeliveryStats)
	var i FederationDeliveryStatsRow
	err := row.Scan(
		&i.Pending,
		&i.Running,
		&i.Done,
		&i.Failed,
		&i.OldestPendingAgeSeconds,
	)
	return i, err
}

const federationRecentFailures = `-- name: FederationRecentFailures :many
SELECT id, last_error AS error, attempts, updated_at
FROM federation_deliveries
WHERE state = 'failed'
ORDER BY updated_at DESC
LIMIT $1
`

type FederationRecentFailuresRow struct {
	ID        uuid.UUID `json:"id"`
	Error     string    `json:"error"`
	Attempts  int32     `json:"attempts"`
	UpdatedAt time.Time `json:"updated_at"`
}

func (q *Queries) FederationRecentFailures(ctx context.Context, limit int32) ([]FederationRecentFailuresRow, error) {
	rows, err := q.db.Query(ctx, federationRecentFailures, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []FederationRecentFailuresRow
	for rows.Next() {
		var i FederationRecentFailuresRow
		if err := rows.Scan(
			&i.ID,
			&i.Error,
			&i.Attempts,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const importJobStats = `-- name: ImportJobStats :one
SELECT
    count(*) FILTER (WHERE state = 'pending')::bigint AS pending,
    count(*) FILTER (WHERE state = 'running')::bigint AS running,
    count(*) FILTER (WHERE state = 'done')::bigint    AS done,
    count(*) FILTER (WHERE state = 'failed')::bigint  AS failed,
    COALESCE(EXTRACT(EPOCH FROM (now() - min(created_at) FILTER (WHERE state = 'pending')))::bigint, 0)::bigint AS oldest_pending_age_seconds
FROM import_jobs
`

type ImportJobStatsRow struct {
	Pending                 int64 `json:"pending"`
	Running                 int64 `json:"running"`
	Done                    int64 `json:"done"`
	Failed                  int64 `json:"failed"`
	OldestPendingAgeSeconds int64 `json:"oldest_pending_age_seconds"`
}

func (q *Queries) ImportJobStats(ctx context.Context) (ImportJobStatsRow, error) {
	row := q.db.QueryRow(ctx, importJobStats)
	var i ImportJobStatsRow
	err := row.Scan(
		&i.Pending,
		&i.Running,
		&i.Done,
		&i.Failed,
		&i.OldestPendingAgeSeconds,
	)
	return i, err
}

const importRecentFailures = `-- name: ImportRecentFailures :many
SELECT id, error, attempts, updated_at
FROM import_jobs
WHERE state = 'failed'
ORDER BY updated_at DESC
LIMIT $1
`

type ImportRecentFailuresRow struct {
	ID        uuid.UUID `json:"id"`
	Error     string    `json:"error"`
	Attempts  int32     `json:"attempts"`
	UpdatedAt time.Time `json:"updated_at"`
}

func (q *Queries) ImportRecentFailures(ctx context.Context, limit int32) ([]ImportRecentFailuresRow, error) {
	rows, err := q.db.Query(ctx, importRecentFailures, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ImportRecentFailuresRow
	for rows.Next() {
		var i ImportRecentFailuresRow
		if err := rows.Scan(
			&i.ID,
			&i.Error,
			&i.Attempts,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const transcodeJobStats = `-- name: TranscodeJobStats :one

SELECT
    count(*) FILTER (WHERE state = 'pending')::bigint AS pending,
    count(*) FILTER (WHERE state = 'running')::bigint AS running,
    count(*) FILTER (WHERE state = 'done')::bigint    AS done,
    count(*) FILTER (WHERE state = 'failed')::bigint  AS failed,
    COALESCE(EXTRACT(EPOCH FROM (now() - min(created_at) FILTER (WHERE state = 'pending')))::bigint, 0)::bigint AS oldest_pending_age_seconds
FROM transcode_jobs
`

type TranscodeJobStatsRow struct {
	Pending                 int64 `json:"pending"`
	Running                 int64 `json:"running"`
	Done                    int64 `json:"done"`
	Failed                  int64 `json:"failed"`
	OldestPendingAgeSeconds int64 `json:"oldest_pending_age_seconds"`
}

// Job-status aggregation for the admin operations dashboard (fix_plan P17.4).
// One aggregate query per durable queue returns the normalised
// {pending, running, done, failed, oldest_pending_age_seconds} shape the admin
// jobs page consumes; a per-queue recent-failures query lists the latest dead
// rows (id, error, attempts) with NO secrets (never the inbox URL, source URL,
// storage key, or arguments). Queues use slightly different state vocabularies
// (delivered vs done; active/completed/cancelled for sessions); each query
// normalises to the common four states here so the service layer stays trivial.
func (q *Queries) TranscodeJobStats(ctx context.Context) (TranscodeJobStatsRow, error) {
	row := q.db.QueryRow(ctx, transcodeJobStats)
	var i TranscodeJobStatsRow
	err := row.Scan(
		&i.Pending,
		&i.Running,
		&i.Done,
		&i.Failed,
		&i.OldestPendingAgeSeconds,
	)
	return i, err
}

const transcodeRecentFailures = `-- name: TranscodeRecentFailures :many
SELECT id, last_error AS error, attempts, updated_at
FROM transcode_jobs
WHERE state = 'failed'
ORDER BY updated_at DESC
LIMIT $1
`

type TranscodeRecentFailuresRow struct {
	ID        uuid.UUID `json:"id"`
	Error     string    `json:"error"`
	Attempts  int32     `json:"attempts"`
	UpdatedAt time.Time `json:"updated_at"`
}

func (q *Queries) TranscodeRecentFailures(ctx context.Context, limit int32) ([]TranscodeRecentFailuresRow, error) {
	rows, err := q.db.Query(ctx, transcodeRecentFailures, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TranscodeRecentFailuresRow
	for rows.Next() {
		var i TranscodeRecentFailuresRow
		if err := rows.Scan(
			&i.ID,
			&i.Error,
			&i.Attempts,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const uploadSessionStats = `-- name: UploadSessionStats :one
SELECT
    count(*) FILTER (WHERE state = 'active')::bigint    AS pending,
    0::bigint                                           AS running,
    count(*) FILTER (WHERE state = 'completed')::bigint AS done,
    count(*) FILTER (WHERE state = 'cancelled')::bigint AS failed,
    COALESCE(EXTRACT(EPOCH FROM (now() - min(created_at) FILTER (WHERE state = 'active')))::bigint, 0)::bigint AS oldest_pending_age_seconds
FROM upload_sessions
`

type UploadSessionStatsRow struct {
	Pending                 int64 `json:"pending"`
	Running                 int64 `json:"running"`
	Done                    int64 `json:"done"`
	Failed                  int64 `json:"failed"`
	OldestPendingAgeSeconds int64 `json:"oldest_pending_age_seconds"`
}

func (q *Queries) UploadSessionStats(ctx context.Context) (UploadSessionStatsRow, error) {
	row := q.db.QueryRow(ctx, uploadSessionStats)
	var i UploadSessionStatsRow
	err := row.Scan(
		&i.Pending,
		&i.Running,
		&i.Done,
		&i.Failed,
		&i.OldestPendingAgeSeconds,
	)
	return i, err
}
