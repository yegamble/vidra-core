diff --git a/tests/e2e/helpers.go b/tests/e2e/helpers.go
index 3b4eb0a..1d40cab 100644
--- a/tests/e2e/helpers.go
+++ b/tests/e2e/helpers.go
@@ -87,10 +87,17 @@ func (c *TestClient) RegisterUser(t *testing.T, username, email, password string
 	body, err := json.Marshal(payload)
 	require.NoError(t, err)
 
+	t.Logf("Registering user: username=%s, email=%s", username, email)
+
 	resp, err := c.Post("/auth/register", "application/json", bytes.NewReader(body))
 	require.NoError(t, err)
 	defer func() { _ = resp.Body.Close() }()
 
+	if resp.StatusCode != http.StatusCreated {
+		respBody, _ := io.ReadAll(resp.Body)
+		t.Fatalf("User registration failed with %d: %s", resp.StatusCode, string(respBody))
+	}
+
 	require.Equal(t, http.StatusCreated, resp.StatusCode, "User registration failed")
 
 	// Parse response envelope
@@ -107,6 +114,8 @@ func (c *TestClient) RegisterUser(t *testing.T, username, email, password string
 	err = json.NewDecoder(resp.Body).Decode(&envelope)
 	require.NoError(t, err)
 
+	t.Logf("Registration succeeded: userID=%s", envelope.Data.User.ID)
+
 	c.Token = envelope.Data.AccessToken
 	c.UserID = envelope.Data.User.ID
 
@@ -122,6 +131,8 @@ func (c *TestClient) Login(t *testing.T, username, password string) (userID, tok
 		email = username + "@example.com"
 	}
 
+	t.Logf("Login attempt: username=%s, email=%s", username, email)
+
 	payload := map[string]interface{}{
 		"email":    email, // Changed from "username" to "email"
 		"password": password,
@@ -134,6 +145,27 @@ func (c *TestClient) Login(t *testing.T, username, password string) (userID, tok
 	require.NoError(t, err)
 	defer func() { _ = resp.Body.Close() }()
 
+	if resp.StatusCode != http.StatusOK {
+		respBody, _ := io.ReadAll(resp.Body)
+		t.Logf("Login failed with %d: %s", resp.StatusCode, string(respBody))
+
+		// Try to parse error response for more details
+		var errEnvelope struct {
+			Error struct {
+				Code    string `json:"code"`
+				Message string `json:"message"`
+				Details string `json:"details"`
+			} `json:"error"`
+			Success bool `json:"success"`
+		}
+		if err := json.Unmarshal(respBody, &errEnvelope); err == nil {
+			t.Logf("Error code: %s, Message: %s, Details: %s",
+				errEnvelope.Error.Code,
+				errEnvelope.Error.Message,
+				errEnvelope.Error.Details)
+		}
+	}
+
 	require.Equal(t, http.StatusOK, resp.StatusCode, "User login failed")
 
 	// Parse response envelope
@@ -150,6 +182,8 @@ func (c *TestClient) Login(t *testing.T, username, password string) (userID, tok
 	err = json.NewDecoder(resp.Body).Decode(&envelope)
 	require.NoError(t, err)
 
+	t.Logf("Login succeeded: userID=%s", envelope.Data.User.ID)
+
 	c.Token = envelope.Data.AccessToken
 	c.UserID = envelope.Data.User.ID
 
@@ -182,7 +216,7 @@ func (c *TestClient) UploadVideo(t *testing.T, videoPath, title, description str
 	require.NoError(t, err)
 
 	// Send request
-	req, err := http.NewRequest("POST", c.BaseURL+"/api/v1/videos", &buf)
+	req, err := http.NewRequest("POST", c.BaseURL+"/api/v1/videos/upload", &buf)
 	require.NoError(t, err)
 
 	req.Header.Set("Content-Type", writer.FormDataContentType())
