# Vidra Extensions Ledger

Vidra-specific features are tracked separately from PeerTube parity so Ralph does not confuse extras with completion of parity.

> Close-out reconciliation 2026-07-03: in-scope backend rows with a green backend-backed Playwright spec (the `vidra-user` `e2e-backed/` suite, 74 passed / 0 failed) are flipped to `VERIFIED`.
>
> IPFS update 2026-07-07: `VIDRA-IPFS-STORAGE` shipped end to end (P19.1–P19.6, public-only v1 per product-decisions.md §5) and is `VERIFIED` on the **backend** (unit + handler + tagged integration + `make ci` green). The remaining work is the **cross-repo vidra-user** surface (truthful IPFS badge on `VideoCard`, admin status/config panel) which consumes the now-shipped `ipfs_pinned` field + `/ipfs/status` endpoint — tracked in the frontend column below, not a vidra-core blocker.
>
> IPFS PRIVATE tier update 2026-07-08: the **private mirroring tier** (P19.P1–P19.P4, spec `.ralph/specs/ipfs-media-private.md`, DECISION product-decisions.md §5.P) also shipped and is `VERIFIED` on the **backend** — a second, fully separate swarm.key'd node replicates non-public media (private/unlisted videos + derivatives, non-public covers, unlisted/deactivated-owner images) with fail-closed routing (a private row is provably incapable of pinning via the public client, and no private CID reaches any API payload / gateway URL). **No encryption layer** this phase (§5.P records the verdicts as DECISIONS: Class B encrypt-then-pin-publicly REJECTED, Class A e2ee-blobs → private DEFERRED, Lit REJECTED, hosted Pinata NOT adopted). The cross-repo vidra-user AdminIpfsView `networks{public,private}` column is the remaining frontend surface (tracked in that repo).

| id | feature | scope | backend requirements | frontend requirements | tests required | status | evidence | notes |
|---|---|---|---|---|---|---|---|---|
| VIDRA-ATPROTO | ATProto/Bluesky outbound cross-posting (v1) | federation | `ATPROTO_ENABLED` (default off, independent of AP; 4-combo boot matrix); `ATPROTO_KEY_KEK` (falls back to FEDERATION_KEY_KEK) seals app passwords; migration 0066 `atproto_accounts` + `atproto_posts` queue; `PUT/GET/DELETE /api/v1/me/atproto`; SSRF-guarded https+public PDS client (createSession/uploadBlob/createRecord); publish-hook enqueue + ticker worker (429 backoff, dead-letter 6) | Settings → Connected accounts: link/unlink Bluesky, auto-post toggle, last-post status (frontend, vidra-user) | unit (sealing, post-record, queue FSM w/ fake XRPC), handler (httptest fake PDS, secret-hiding), config matrix, integration (queue vs real PG) — all green | VERIFIED | core-atproto slice 2026-07-03: migration 0066, `internal/atproto`, `internal/httpapi/atproto.go`, config `ATPROTO_ENABLED`/`ATProtoKEK()`, openapi + TestOpenAPIContract, `make ci` + `-tags=integration -race` green (fix_plan P10.2); vidra-user Connected-accounts settings; backed `atproto.spec.ts` | Must not break ActivityPub parity; no PDS hosting / inbound firehose / DID login in v1 |
| VIDRA-IPFS-STORAGE | Hybrid IPFS media mirroring (public v1 + private-swarm tier) | storage/media | Mirror sidecar (NOT an authoritative backend — `STORAGE_BACKEND=ipfs` stays rejected): `media_ipfs_pins` pin ledger + durable queue (migration 0071), `IPFS_*` config surface with the hard privacy guard, `internal/ipfs` Kubo RPC client + CID validation, `internal/ipfsmirror` worker + eligibility gate (public-only), additive `ipfs`/`ipfs_pinned` API fields, admin `/ipfs/status` + `/admin/ipfs/reconcile`, optional cluster replication. **Private tier (P19.P):** `network` column (migration 0077) + `target_network` flip column (0078), dedicated second-swarm client + fail-closed routing, eligibility matrix v2 (`Route`), privacy-flip network transitions, network-aware backfill/reconcile (`?network=` filter), additive `/ipfs/status` `networks{public,private}` split (per-swarm node+cluster health), `ipfs-private` compose profile + swarm.key custody. | IPFS status/config admin panel + truthful IPFS badge (vidra-user W0.4 `VideoCard.tsx:12`, cross-repo dependency on the `ipfs_pinned` field); AdminIpfsView `networks{public,private}` column (private tier, cross-repo) | unit (fake node, eligibility privacy table incl. class×privacy×network `TestRouteTable`, CID fuzz, fail-closed routing, privacy-flip transitions, network-aware backfill), integration (`//go:build ipfs_integration` public; `//go:build ipfs_private_integration` swarm.key isolation + fail-closed-boot proofs), handler | VERIFIED (backend) | **Public v1 (P19.1–P19.6, user-approved 2026-07-07):** spec `.ralph/specs/ipfs-media.md`. Migration **0071** `media_ipfs_pins` + sqlc; `IPFS_*` config + hard privacy guard; `internal/ipfs` Kubo client/CID/fake + optional cluster client; `internal/ipfsmirror` worker + eligibility privacy fence (`TestEligibilityTable`), reconciliation, video/HLS pin lifecycle (`TestHLSPinLifecycle`, `TestReferenceCheckedUnpin`); additive `ipfs`/`ipfs_pinned` fields; admin `GET /api/v1/ipfs/status` (incl. `cluster_reachable`); compose `ipfs` profile + `ipfs_integration` round-trip; one-shot `POST /api/v1/admin/ipfs/reconcile` backfill (`BackfillIPFSPinIntent`, admin-gated + audited `admin.ipfs.reconcile`, idempotent: `TestBackfillSeedsEligibleAndFencesNonPublic`/`TestBackfillIdempotent`/`TestIPFSReconcileEnabled`). **Private tier (P19.P1–P19.P4, DECISION product-decisions.md §5.P, spec `.ralph/specs/ipfs-media-private.md`):** config reframe `IPFS_MIRROR_PRIVATE ⇒ IPFS_PRIVATE_API_URL` (dedicated node; dual-home rejected; no `IPFS_PRIVATE_GATEWAY_URL`; optional `IPFS_PRIVATE_CLUSTER_API_URL`/`_TOKEN` secret) with `TestLoadIPFSValidation`; migration **0077** `network`(`public`/`private`) + `(network,state,media_class)` index and **0078** `target_network` flip column (both real-Postgres smoke-verified, up→down→up clean); dual-client fail-closed worker (`TestPrivateRowRoutedToPrivateClientNeverPublic` + reverse — private row never pins via the public client and vice versa); eligibility matrix v2 `Route` class×privacy×network `TestRouteTable`; privacy-flip transitions (`TestSyncVideoPublicToPrivateTransition`, `TestFlipToPrivateRacingInFlightPublicPinNeverPinsPublic`, `TestReferenceCheckScopedPerNetwork`, `TestDeleteUnpinsOnHoldingNetwork`); network-aware one-shot reconcile (`TestBackfillRoutesPrivateEligibleToPrivateSwarm` — private-eligible objects seeded on the private swarm, quarantined nowhere, `?network=` scopes the seed; handler `TestIPFSReconcileNetworkFilter`); admin `networks{public,private}` split with independent per-swarm node+cluster health (`TestIPFSStatusEnabled`, `TestStatusPrivateClusterHealthPerSwarm`); no private CID/gateway URL in any payload (`TestVideoPinsNeverExposesPrivateNetworkCID`, `TestVideoDetailNoCIDForNonPublic`); `ipfs-private` compose profile (`LIBP2P_FORCE_PNET=1`, bootstrap cleared, `Routing.Type=none`, `Gateway.NoFetch`, gateway NOT host-published) + tagged isolation/fail-closed-boot integration proofs (`TestPrivateSwarmReplication`, `TestOutsideNodeCannotFetch`, `TestKeylessDaemonRefusesToStart`, `TestPrivateClusterReplicationPinned`); `docs/operations.md` private-tier runbook (swarm.key custody: generation/manual distribution/rotation=coordinated restart/possession=membership; DR-restore via `?network=private`). `make ci` green (fmt-check, vet, openapi-verify, sqlc-verify, test-race) at every P19.P slice. | Privacy invariant: nothing non-public is EVER enqueued for a PUBLIC pin; the private tier replicates non-public media only to a **separate swarm.key'd node**, never dual-homed, fail-closed (failure mode is "private content unreachable", never "public"); private CIDs never appear in any API response / gateway URL. **No encryption layer** (product-decisions.md §5.P: Class B rejected, Class A e2ee-blobs deferred-private, Lit rejected, hosted Pinata not adopted); e2ee-blob pinning stays a recorded deferral pending the Messaging v2 E2EE-attachment slice. **Cross-repo dependency:** vidra-user IPFS badge (`VideoCard.tsx:12`) + admin status panel (`networks{public,private}` split) consume the shipped `ipfs_pinned`/`/ipfs/status` surface. |
| VIDRA-MSG-NORMAL | Normal secure messaging | messaging | server-side message body, attachments, SSRF-safe previews, read receipts, per-message delete/report | inbox/thread/composer/attachments/link previews/read receipts UI | integration, Playwright, security | VERIFIED | migration 0031 + **0064** (core-dm-completeness, product-decisions.md §14, 2026-07-03), `internal/messaging` + `internal/linkpreview`, conversations/messages endpoints, block gating (fix_plan P11.1). Attachments (ClamAV fail-closed), link previews (SSRF-guarded OpenGraph), read receipts, per-message delete + report all DONE; vidra-user inbox/thread/composer; backed `messaging.spec.ts`. Typing presence INTENTIONAL_DIFFERENCE (polling) | Separate from PeerTube parity |
| VIDRA-MSG-E2EE | Encrypted messaging | messaging/security | ciphertext-only storage, key/device metadata, test vectors, no homebrew crypto | encryption state UI, composer/thread, no plaintext logging | test vectors, integration, Playwright, security | VERIFIED | threat model + Olm decision `.ralph/specs/e2ee.md`; migration 0058; `internal/e2ee` + §4 endpoints (devices/OTK/atomic claim/envelopes); disappearing sweeper; storage-invariant + concurrent-claim integration tests (fix_plan P11.2, all boxes checked); vidra-user Olm client (device setup, start-encrypted, fingerprints, P8.2); backed `e2ee.spec.ts` | Threat-model gate satisfied |
| VIDRA-WHISPER-CAPTIONS | Whisper auto captions | media/captions | optional worker/config, job status, caption review/edit | caption generation controls/status/edit flow | integration, smoke, Playwright | VERIFIED | core-whisper (2026-07-03): `WHISPER_*` config, migration 0062 `caption_jobs` durable queue, `internal/media.WhisperClient` (ffmpeg wav → whisper.cpp → WebVTT), `internal/captionjob` worker + `caption_ready` notification, `POST`/`GET /videos/{id}/captions/auto`, `captions` compose profile; unit + mocked-integration + real-ffmpeg tests + `make ci`/integration green (fix_plan P13); vidra-user caption controls/status/edit; backed `whisper-captions.spec.ts` + `captions.spec.ts` | Optional by config |
| VIDRA-CLAMAV | Virus scanning | security/media | ClamAV integration, fail-open/closed/quarantine policy, scan states | upload status/error messaging | unit, integration, smoke | VERIFIED | core-media-2 (2026-07-03): `internal/media.ClamAV` INSTREAM scanner; `MALWARE_SCAN_MODE` fail-closed(default)/fail-open/quarantine honored by `video.Process` (infected always fails); compose `scan` profile with clamd; 6 video-service + config tests + API-level scan-failure test; `make ci` green (fix_plan P6.1/P15). The quarantine path (fail on infection / park on error) is proven end-to-end via vidra-user backed `quarantine.spec.ts`; upload status/error messaging surfaced in the studio publish flow | Production defaults fail-closed; quarantine reuses the P9 moderation queue |
| VIDRA-DONATION-WALLET | Simple verified crypto wallet display | profile/settings | address storage, challenge verification, no custody/no payment processing | profile wallet display, settings verification | unit, integration, Playwright | VERIFIED | backend done (`core-donations`, fix_plan P14 all boxes 2026-07-03): migration 0063 `donation_addresses`, `internal/donation` (validators + EIP-191 verify via decred secp256k1), CRUD `/me/donation-addresses`, challenge/verify (eth supported; btc/monero/litecoin `501` unverified-only), public per-user/per-channel reads with `verified` flag, no-custody by design; unit + HTTP + real-DB integration green; vidra-user profile display + settings verification; backed `donations.spec.ts` | Premium/payments remain deferred; NON-CUSTODIAL only |
