# Vidra Extensions Ledger

Vidra-specific features are tracked separately from PeerTube parity so Ralph does not confuse extras with completion of parity.

| id | feature | scope | backend requirements | frontend requirements | tests required | status | evidence | notes |
|---|---|---|---|---|---|---|---|---|
| VIDRA-ATPROTO | ATProto/Bluesky outbound cross-posting (v1) | federation | `ATPROTO_ENABLED` (default off, independent of AP; 4-combo boot matrix); `ATPROTO_KEY_KEK` (falls back to FEDERATION_KEY_KEK) seals app passwords; migration 0066 `atproto_accounts` + `atproto_posts` queue; `PUT/GET/DELETE /api/v1/me/atproto`; SSRF-guarded https+public PDS client (createSession/uploadBlob/createRecord); publish-hook enqueue + ticker worker (429 backoff, dead-letter 6) | Settings → Connected accounts: link/unlink Bluesky, auto-post toggle, last-post status (frontend, vidra-user) | unit (sealing, post-record, queue FSM w/ fake XRPC), handler (httptest fake PDS, secret-hiding), config matrix, integration (queue vs real PG) — all green | BACKEND_PARTIAL | core-atproto slice 2026-07-03: migration 0066, `internal/atproto`, `internal/httpapi/atproto.go`, config `ATPROTO_ENABLED`/`ATProtoKEK()`, openapi + TestOpenAPIContract, `make ci` + `-tags=integration -race` green (see fix_plan P10.2). Frontend link UI pending in vidra-user. | Must not break ActivityPub parity; no PDS hosting / inbound firehose / DID login in v1 |
| VIDRA-IPFS-STORAGE | IPFS storage/distribution | storage/media | IPFS adapter/gateway/pinning, content metadata, fallback modes | IPFS status/config UI where supported | integration, smoke, Playwright | NOT_STARTED | none | Use uploaded Vidra IPFS UX docs when present |
| VIDRA-MSG-NORMAL | Normal secure messaging | messaging | server-side message body, attachments, SSRF-safe previews, read receipts, per-message delete/report | inbox/thread/composer/attachments/link previews/read receipts UI | integration, Playwright, security | BACKEND_PARTIAL | migration 0031 + **0064** (core-dm-completeness, product-decisions.md §14, 2026-07-03), `internal/messaging` + `internal/linkpreview`, conversations/messages endpoints, block gating (fix_plan P11.1). **Attachments** (25 MiB, image/video/audio/pdf allowlist, ClamAV fail-closed, `dm-attachments/<conv>/<id>.<ext>`, participant-gated download, ≤4 per send, plaintext-only → E2EE 422), **link previews** (async SSRF-guarded OpenGraph fetch, 1 MiB, shared cache by URL hash, best-effort, never blocks send), **read receipts** (`last_read_message_id` + `/conversations/:id/read`, inbox unread counts, peer watermark, `notification_prefs`-backed `read_receipts` toggle), **per-message delete** (sender tombstone) **+ report** (target_type=message + body snapshot into admin queue) all DONE. Typing presence INTENTIONAL_DIFFERENCE (polling). Frontend UI pending | Separate from PeerTube parity |
| VIDRA-MSG-E2EE | Encrypted messaging | messaging/security | ciphertext-only storage, key/device metadata, test vectors, no homebrew crypto | encryption state UI, composer/thread, no plaintext logging | test vectors, integration, Playwright, security | BACKEND_PARTIAL | threat model + Olm decision `.ralph/specs/e2ee.md`; migration 0058; `internal/e2ee` + §4 endpoints (devices/OTK/atomic claim/envelopes); disappearing sweeper; storage-invariant + concurrent-claim integration tests (fix_plan P11.2, all boxes checked 2026-07-03). Frontend (Olm client, P8.2) pending | Threat-model gate satisfied |
| VIDRA-WHISPER-CAPTIONS | Whisper auto captions | media/captions | optional worker/config, job status, caption review/edit | caption generation controls/status/edit flow | integration, smoke, Playwright | BACKEND_PARTIAL | core-whisper (2026-07-03): `WHISPER_ENABLED`/`WHISPER_ENDPOINT`/`WHISPER_DEFAULT_LANGUAGE` config; migration 0062 `caption_jobs` durable queue; `internal/media.WhisperClient` (ffmpeg 16k-mono-wav extract → whisper.cpp `/inference` POST → verbose_json→WebVTT); `internal/captionjob` worker (retry/dead-letter) upserting via the existing `video.AddCaption` path + `caption_ready` notification; `POST`/`GET /api/v1/videos/{id}/captions/auto` (202/503/409/422); compose `captions` profile (whisper.cpp). Unit + mocked-integration (httptest fake whisper) + real-ffmpeg extraction tests; `make ci` + `-tags=integration -race` green. Frontend controls/status/edit flow pending | Optional by config |
| VIDRA-CLAMAV | Virus scanning | security/media | ClamAV integration, fail-open/closed/quarantine policy, scan states | upload status/error messaging | unit, integration, smoke | BACKEND_PARTIAL | core-media-2 (2026-07-03): `internal/media.ClamAV` INSTREAM scanner; `MALWARE_SCAN_MODE` fail-closed(default)/fail-open/quarantine honored by `video.Process` (infected always fails); compose `scan` profile with clamd; 6 video-service + config tests; `make ci` green. Frontend upload status messaging pending | Production defaults fail-closed; quarantine reuses the P9 moderation queue |
| VIDRA-DONATION-WALLET | Simple verified crypto wallet display | profile/settings | address storage, challenge verification, no custody/no payment processing | profile wallet display, settings verification | unit, integration, Playwright | BACKEND_PARTIAL | Backend done (`core-donations`, fix_plan P14 all boxes 2026-07-03): migration 0063 `donation_addresses`, `internal/donation` (validators + EIP-191 verify via decred secp256k1), CRUD `/me/donation-addresses`, challenge/verify (eth supported; btc/monero/litecoin `501` unverified-only), public per-user/per-channel reads with `verified` flag, no-custody by design. Unit + HTTP + real-DB integration tests green. Frontend (profile display + settings verification, vidra-user) pending | Premium/payments remain deferred; NON-CUSTODIAL only |
