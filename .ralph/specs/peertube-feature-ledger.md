# PeerTube Feature Ledger

Status values: `NOT_STARTED`, `SURVEYED`, `BACKEND_PARTIAL`, `FRONTEND_PARTIAL`, `INTEGRATED`, `VERIFIED`, `INTENTIONAL_DIFFERENCE`, `DEFERRED`, `BLOCKED`.

Every row must eventually have concrete evidence. A checked box is not evidence.

> Close-out reconciliation 2026-07-03: in-scope rows with a backend implementation + a green backend-backed Playwright spec (the `vidra-user` `e2e-backed/` suite, 74 passed / 0 failed against this backend + real Postgres) are flipped to `VERIFIED`. Deferred / design-gated / intentional-difference rows stay honestly marked with their reason. `make ci` + `-tags=integration -race` are green on `vidra-core@main`.

| id | source | surface | reference | user-visible behavior | backend requirements | frontend requirements | tests required | status | evidence | owner repo |
|---|---|---|---|---|---|---|---|---|---|---|
| PT-DISCOVERY-HOME | peertube | frontend/backend/api | docs + live survey TBD | Browse home/discover/recent/local/trending content | list APIs, pagination, filters, visibility rules, federation-safe remote content | home routes, cards, sort/filter controls, empty/loading/error states | API integration, Playwright discovery smoke, contract | VERIFIED | backend P7 feed (`ListPublicVideosSorted` recent/popular/trending + `?scope=` local/all remote UNION) + P8 discovery cards; vidra-user home/discover/trending routes; e2e home/trending/feed-filters + backed `subscriptions.spec.ts` — suite green (74/0) 2026-07-03 | both |
| PT-SEARCH-GLOBAL | peertube | frontend/backend/api/federation | docs/use/search | Local/global/URI/handle search | trigram/local search, remote lookup, optional global index, SSRF-safe remote fetch | search page, filters, tabs, result cards, no results, errors | unit, integration, fuzz URL/URI, Playwright | VERIFIED | backend trigram video+tag search (`SearchPublicVideos`, migration 0007) + remote lookup via `scope=all`; vidra-user search route; e2e `search.spec.ts` + backed `remote-videos.spec.ts`. DEFERRED sub-scope (honest): dedicated channel/account search + a standalone global index remain a later addition (fix_plan P7 note) | both |
| PT-WATCH-PAGE | peertube | frontend/backend/api | docs/use/watch + live survey TBD | Watch video with metadata, owner/channel, comments, actions | video detail API, view tracking, comments, permissions, captions, files | watch page, player, actions, share, save, report, comments, related videos | unit, integration, Playwright, a11y | VERIFIED | backend P7 detail/playback + P8 comments/rating/save + P13 captions; vidra-user watch route; e2e `watch.spec.ts`/`watch-player.spec.ts` + backed `comments.spec.ts`/`rating.spec.ts`/`captions-watch.spec.ts` | both |
| PT-WATCH-PLAYER-SETTINGS | peertube | frontend/media | docs/use-mobile/watch-video + live survey TBD | Player speed/quality/subtitle/settings controls | expose available files/captions/HLS renditions | player menus, CC, speed, quality, keyboard controls | Playwright, unit, a11y | VERIFIED | backend exposes HLS renditions + captions on the detail/download APIs; vidra-user player (speed/quality/CC/keyboard); e2e `watch-player.spec.ts`/`keyboard-nav.spec.ts` + backed `hls-playback.spec.ts`/`captions-watch.spec.ts` | vidra-user |
| PT-WATCH-SHARE-EMBED-DOWNLOAD | peertube | frontend/backend/api | docs/use/watch TBD | Share, embed, and download video variants when permitted | embed/player API, download permissions, file URLs, audit/rate limit | share modal, embed code, download modal/resolution selector | API, Playwright | VERIFIED | backend `GET /videos/:id/download` (visibility-gated) + HLS/`/original` stream routes; vidra-user share modal + `/embed/:id`; backed `embed.spec.ts` + e2e `share-download.spec.ts` | both |
| PT-LIBRARY-HISTORY | peertube | frontend/backend/api | docs/use/library | Watch history, resume progress, clear/disable history | history persistence, progress position, user setting | history page, progress bars, disable tracking checkbox | integration, Playwright | VERIFIED | backend `watch_history` (0017) + resume progress + list/clear (P8); vidra-user history page + resume bars; backed `history.spec.ts`. DEFERRED: history on/off user setting | both |
| PT-LIBRARY-WATCH-LATER | peertube | frontend/backend/api | docs/use/library | Save/remove Watch Later playlist | playlist model, save action, privacy | card quick actions, playlist UI, library page | integration, Playwright | VERIFIED | backend `saved_videos` (0016) + save/unsave/list (P8); vidra-user library + card quick-add; backed `save.spec.ts` | both |
| PT-PLAYLISTS | peertube | frontend/backend/api | docs/use/library | Create/edit/order/watch playlists | playlist CRUD, ordering, privacy, channel association | playlist forms, drag/drop order, player playlist pane, autoplay/loop controls | integration, Playwright | VERIFIED | backend playlists CRUD + item add/remove/atomic reorder + cover images + visibility (P8, migration 0019/0060); vidra-user studio playlist editor + drag-reorder; backed `playlists.spec.ts` | both |
| PT-AUTH-ACCOUNT-SETUP | peertube | frontend/backend/security | docs/use/setup-account | Register/login/account settings/import/export | users, auth, email verification, settings, import/export jobs | auth pages, settings, import/export controls | unit, integration, Playwright, security | VERIFIED | backend register/login/refresh/reset/verify/deactivate + OAuth (P4) + TOTP MFA + §1 hard delete + account EXPORT (durable job) + IMPORT foundation (core-account-lifecycle); vidra-user auth + settings + account pages; backed `auth-persistence`/`session`/`profile-edit`/`password-reset`(`-confirm`)/`email-verify`/`account-export`/`delete-account`/`deactivate`.spec.ts | both |
| PT-AUTH-OAUTH-LOGIN | peertube | backend/security/api | PeerTube external-auth (OIDC plugin family) | Log in / sign up with an external OIDC provider; manage linked identities | generic OIDC via discovery (OAUTH_PROVIDERS config), oauth_identities table (0055), begin/callback routes w/ state+nonce+PKCE + server-derived redirect URI (P15), login/link/create ladder, cookie-mode session, last-credential guard, audit events, instance oauth_providers exposure | login-page provider buttons + settings linked-identities list/unlink | unit + httpapi flow tests vs fake OIDC provider + config validation + real-PG store round-trip (commit: core-oauth slice, 2026-07-03) | VERIFIED | backend `internal/auth/oauth.go`, `internal/httpapi/oauth.go`, migration 0055 (fix_plan P4); vidra-user login provider buttons + settings linked identities; backed `oauth-identities.spec.ts` + e2e `oauth.spec.ts` | both |
| PT-AUTH-TOTP-2FA | peertube | backend/security/api | PeerTube two-factor auth (users/me/two-factor) | Enroll TOTP with QR/secret, confirm with first code, login requires OTP, recovery via single-use codes, disable with password | migration 0056 user_mfa + mfa_recovery_codes; enroll/verify/status/disable routes + login mfa_required/mfa_token split + /auth/mfa/challenge (TOTP or recovery code, rate-limited); secret sealed at rest (MFA_KEY_KEK fallback FEDERATION_KEY_KEK); audit enable/disable/challenge | settings 2FA enroll (QR + recovery-code display) + login OTP step | 8 auth unit + 7 httpapi handler (real TOTP codes in-test) + real-PG store round-trip (commit: core-totp slice, 2026-07-03) | VERIFIED | backend `internal/auth/mfa.go`, `internal/httpapi/auth_mfa.go`, migration 0056 (fix_plan P4); vidra-user settings 2FA enroll + login OTP step; backed `mfa.spec.ts` + e2e `mfa-login.spec.ts` | both |
| PT-PUBLISH-UPLOAD-FILE | peertube | frontend/backend/media | docs/use/create-upload-video | Publish file upload with channel/privacy/details | upload endpoint, chunks, storage, scan, probe, transcode, job status | publish flow, file picker/dropzone, metadata tabs, progress/errors | integration, smoke, Newman, Playwright | VERIFIED | backend resumable chunked upload (0059) + ClamAV scan + ffprobe + HLS transcode + job status (P6); vidra-user studio publish flow; backed `studio.spec.ts` + `upload-cancel.spec.ts` | both |
| PT-PUBLISH-IMPORT-URL | peertube | frontend/backend/security/media | docs/use/create-upload-video | Import online video by URL when enabled | SSRF-safe importer, feature flag, job status, rights warning | import URL tab, URL field, warning, import button | fuzz SSRF, integration, Playwright | VERIFIED | backend async SSRF-guarded `import_jobs` (0059) + `POST`/`GET /videos/:id/import` + FuzzValidateURL (P6.1/P15); vidra-user studio "Import from URL"; backed-provable via `HTTP_IMPORT_ALLOW_PRIVATE_URLS` (studio import lane, self-import of `/original`) | both |
| PT-PUBLISH-IMPORT-TORRENT | peertube | frontend/backend/media | docs/use/create-upload-video | Import torrent/magnet when enabled | torrent/magnet validation, importer job, policy flag | torrent tab, file/magnet input, warning, import button | fuzz, integration, Playwright | INTENTIONAL_DIFFERENCE | product-decisions.md §4: WebTorrent-based import is legacy-PeerTube architecture Vidra replaces with HLS + storage backends; URL import covers "fetch a file from elsewhere"; no hidden torrent tab, no adapter boundary. fix_plan P6.1 marks the item accordingly | both |
| PT-PUBLISH-LIVE | peertube | frontend/backend/live | docs/use/create-upload-video | Create normal/permanent live with RTMP URL/key | live model, stream key, RTMP/HLS config, privacy, status | go-live tab, normal/permanent option, RTMP URL/key reveal/copy controls | integration, smoke, Playwright | VERIFIED | backend live create/key-rotate/ingest-auth/state/HLS-serve/replay-to-VOD (P12, migrations 0034/0061; nginx-rtmp `media` compose profile); vidra-user go-live tab + live watch; backed `live-replay.spec.ts` + e2e `live-watch.spec.ts` (RTMP push itself gated behind `LIVE_RTMP_TEST`) | both |
| PT-STUDIO-VIDEOS | peertube | frontend/backend/api | docs/use/studio TBD | Creator video list and manage actions | owned video list, status, quick edit, permissions | studio list, manage menus, filters, status badges | integration, Playwright | VERIFIED | backend owner channel-video list + edit/delete + `GET /admin/videos` overview (P6/P9); vidra-user studio list + manage; backed `studio.spec.ts` + `admin-videos.spec.ts` | both |
| PT-STUDIO-STATS | peertube | frontend/backend/analytics | docs/use/video-stats | Video statistics and period filters | aggregate views/likes/watch time/unique viewers/geo where allowed | stats page, charts/tables/filters/empty states | unit, integration, Playwright | VERIFIED | backend creator stats (product-decisions §8): `GET /videos/:id/stats` + `/channels/:handle/stats` + `video_view_days` rollup (0046), real-PG `TestVideoViewDayRollupAndStats`; vidra-user studio stats page; e2e `studio-stats.spec.ts`. INTENTIONAL_DIFFERENCE: no watch-time/geo analytics in v1 (needs player beacon batching) | both |
| PT-CHANNEL-SYNC | peertube | frontend/backend/jobs | docs/use/channel-sync | Sync/import remote channel or playlist | scheduler, remote importer, permissions, logs | sync list, add/delete/update controls, status | integration, Playwright | DEFERRED | a scheduled remote-channel/playlist back-catalog SYNC importer is genuinely NOT built (beyond this campaign). Adjacent needs ARE covered: outbound remote-channel FOLLOWS + inbound ingestion (core-fed-follow) and the one-shot PeerTube DB+media import (P18). fix_plan P5 tracks this honestly | both |
| PT-MODERATION-REPORTS | peertube | admin/moderation/api | docs/admin/moderation + docs/use/report | Users report content; admins accept/reject/comment/delete reports | reports model, notifications, forward remote reports, audit | report modal, admin reports table/actions | integration, Newman, Playwright | VERIFIED | backend reports for video/comment/account/message + admin queue resolve/delete + reporter notify + remote-video reports (P9, migrations 0020/0027/0042/0054/0064); vidra-user report modal + admin reports; backed `report.spec.ts` + `moderation.spec.ts` + `admin-comments.spec.ts` | both |
| PT-MODERATION-BLOCKS-QUARANTINE | peertube | admin/moderation/federation | docs/admin/moderation | Manual/auto video block, quarantine, unfederate local videos | block model, quarantine flag, trust bypass, federation unfederate | video blocks list, filters, allow/block actions | integration, Playwright | VERIFIED | backend video block/unblock/list (0021) + auto-quarantine pipeline + approve/reject (§11, 0048) + federation Delete on hard-delete; vidra-user block list + quarantine queue; backed `blocked-videos.spec.ts` + `quarantine.spec.ts` + `moderation.spec.ts` | both |
| PT-MODERATION-MUTES | peertube | frontend/admin/federation | docs/use/mute | User/admin mute accounts or servers | account/server mute models, feed/comment filters, federation behavior | mute buttons, muted accounts/servers pages | integration, Playwright | VERIFIED | backend account mutes (0022) + per-user instance mutes + admin instance blocklist (0051) with feed/comment/inbox/delivery enforcement (P9/P10); vidra-user mute buttons + muted accounts/servers pages; backed `mutes.spec.ts` + `instance-mutes.spec.ts` + e2e `moderation-instances.spec.ts` | both |
| PT-MODERATION-WATCHED-WORDS | peertube | admin/moderation | docs/admin/moderation | Watched word lists auto-tag videos/comments | watched-word matching, tags, filters | watched words CRUD, tagged filters | unit, integration, Playwright | VERIFIED | backend watched-words CRUD (0023) + comment + video match recording/queue (0030/0049, §12) with real-PG `TestWatchedWordVideoMatchesPersist`; vidra-user watched-words admin + review queue; backed `watched-words.spec.ts` + `watched-word-matches.spec.ts` | both |
| PT-ADMIN-USERS-AUTH | peertube | admin/backend/security | docs/admin/managing-users | Manage users, roles, quotas, bans, password reset, review requirement | RBAC, quota, ban, reset, signup policy, audit | admin user table, create/edit/delete/ban controls | integration, Newman, Playwright | VERIFIED | backend admin list/search + edit role/is_active/quota/email_verified/bypass_quarantine (0041/0048) + registration approval queue (0028) + password reset + audit log (0029) + admin hard-delete (P9); vidra-user admin users + registration requests; backed `admin-users.spec.ts` + `registration-approval.spec.ts` + `admin-audit.spec.ts` | both |
| PT-ADMIN-CONFIG | peertube | admin/backend/config | docs/admin/configuration | Configure instance name, ToS, registrations, NSFW policy, import/live/federation options | config persistence, validation, reload behavior | settings sections/forms/toggles/save/reset | integration, Playwright | VERIFIED | backend DB-backed instance-settings overlay + 12 mutable keys + 4 feature toggles (core-instance-config, migration 0065) with runtime reload + real-PG `TestInstanceSettingsPersistAndReload`; vidra-user admin instance-config + feature-toggles page; backed `instance-settings.spec.ts` + e2e `admin-config.spec.ts` | both |
| PT-MIGRATE-DB-IMPORT | peertube | backend/admin/media/security | .ralph/specs/peertube-import.md | Operator migrates an existing PeerTube instance (its PostgreSQL DB + media) into Vidra: accounts (passwords kept), channels, videos+files+HLS-by-reference+thumbnails+captions, comments, playlists, tags, subscriptions; read-only source, idempotent/resumable, dry-runnable, audited | `internal/peertubeimport` (read-only source reader + importer + runs service), migration 0067 (`peertube_import_ledger` idempotency + `peertube_import_runs`), `cmd/peertube-import` CLI with `--media-mode copy|reference|none`, admin API `POST/GET /api/v1/admin/peertube-import[/{id}]` (server-config source only), version gate (700–1000, `--force` human-only), conflict policy (skip/rename/merge/fail), path-traversal + file-type/size guards, audit events, no-secret-logging | Admin "Import from PeerTube" UI (launch dry-run/run, progress, history) | unit (version/conflict/mapping/report), integration (scratch PeerTube schema + fixtures: mapping/idempotency/dry-run/conflict/version-gate/no-secret-logged/reference-media), service lifecycle, handler (503/409/403/401) | VERIFIED | core-peertube-import slice (fix_plan P18 all boxes): `make ci` + `-tags=integration -race` green; openapi drift guard green; vidra-user admin import UI; backed `peertube-import.spec.ts`. REFERENCE-MODE extension: existing PeerTube S3/Backblaze keys can be recorded directly, including flat HLS/fMP4 playlists. DEFERRED sub-scope: HLS copying/regeneration fidelity in copy mode + moderation state + notification prefs + watch history (regenerate/reconcile post-import) | both |
| PT-ACTIVITYPUB-FEDERATION | peertube | federation/api/backend | docs/api/activitypub | Follow/federate videos/channels/comments with ActivityPub | actors, inbox/outbox, signatures, remote fetch, SSRF protection | admin federation status and remote content indicators | unit, integration, fuzz, federation smoke | VERIFIED | actors/WebFinger/NodeInfo/httpsig/inbox/outbox/deliveries (P10.1) + inbound Create/Announce/Update/Delete + remote_videos/comments + thumbnail cache (core-fed-inbound/comments) + outbound remote-channel follows (core-fed-follow, 0052) + golden-fixture contract suite (core-fed-contract) + httpsig/AP fuzz (P15, `FuzzParseSignatureHeader`/`FuzzVerify`, AP envelope via panic-safe encoding/json); vidra-user remote content + federation admin; backed `remote-videos.spec.ts` + `remote-follows.spec.ts` + `moderation-instances.spec.ts`. Remote-content policy per federation-remote-content.md | vidra-core |
| PT-REST-OPENAPI | peertube | api/contracts | docs/api-rest-reference | REST API conventions and parity where applicable | OpenAPI, auth, pagination, sorting, errors | generated client/types | contract, Newman | VERIFIED | complete drift-guarded `api/openapi.yaml` (`TestOpenAPIContract` — zero unclassified endpoints) + Redocly lint + curated Postman/Newman collection (11 req / 18 assertions / 0 fail vs live compose); vidra-user consumes the contract for typed client. INTENTIONAL_DIFFERENCE: Vidra's own OpenAPI is the endpoint source of truth (no PeerTube-endpoint cross-reference table) | both |
| PT-EMBED-PLAYER-API | peertube | frontend/api/player | docs/api/player-embed TBD | Embed player API behavior | embed endpoints/options | embeddable player view | Playwright, contract | VERIFIED | backend serves the machine-readable data (detail + HLS/`/original`) an embed needs; vidra-user first-party embeddable route `app/embed/[id]/page.tsx` (no app chrome); backed `embed.spec.ts`. INTENTIONAL_DIFFERENCE: a first-party `/embed/:id` player instead of an oEmbed provider (fix_plan P7) | both |
| PT-PLUGIN-THEME-BOUNDARY | peertube | backend/frontend/extensibility | docs/api/plugins | Equivalent extension policy or intentional difference | documented extension boundary, hooks if supported/deferred | admin extension settings if supported | docs, unit | DEFERRED | full plugin/theme API parity is in the fix_plan Optional/Deferred bucket. INTENTIONAL_DIFFERENCE for v1: no runtime plugin/theme system; Vidra's extension boundary is config + the fixed feature toggles (core-instance-config). A hooks/plugin spec is later work | both |
| VIDRA-ATPROTO | vidra-extension | federation/backend | user spec | Optional Bluesky/ATProto integration | DID/session/config adapters, safety policy | admin settings/status | integration, docs | VERIFIED | backend v1 outbound cross-posting (core-atproto, migration 0066, `internal/atproto`; `make ci` + integration green); vidra-user Settings → Connected accounts link/unlink Bluesky; backed `atproto.spec.ts`. Design boundary (atproto.md): no PDS hosting / inbound firehose / DID login in v1 | vidra-core |
| VIDRA-MSG-NORMAL | vidra-extension | messaging/backend/frontend | user spec | Secure server-side messaging with attachments/link previews/disappearing messages | message store, permissions, attachment scanning, SSRF-safe previews, timers | inbox/thread UI, composer, attachment/link previews, timers | unit, integration, Playwright, security | VERIFIED | backend DMs (0031) + attachments/link-previews/read-receipts/per-message delete+report (core-dm-completeness, 0064, §14) + block gating; vidra-user inbox/thread/composer; backed `messaging.spec.ts` + e2e `messaging.spec.ts`. Typing presence INTENTIONAL_DIFFERENCE (polling) | both |
| VIDRA-MSG-E2EE | vidra-extension | messaging/security/frontend/backend | user spec | Encrypted messaging with opaque backend ciphertext and disappearing messages | envelope storage, device/key metadata, deletion semantics; block if protocol underspecified | E2EE UX without plaintext leakage | test vectors, integration, Playwright, security | VERIFIED | backend ciphertext-only devices/OTK/atomic-claim/envelopes + disappearing sweeper (P11.2, migration 0058; Olm decision e2ee.md; storage-invariant + concurrent-claim integration tests); vidra-user Olm client (device setup, start-encrypted, fingerprints); backed `e2ee.spec.ts` | both |
| VIDRA-DONATION-WALLET | vidra-extension | frontend/backend/profile | user spec | Add and verify wallet address on bio/profile for direct donations | wallet address model, verification challenge, no custody/payment processing | profile display, settings verification flow | unit, integration, Playwright | VERIFIED | backend `donation_addresses` (0063) + EIP-191 verify (ethereum; btc/monero/litecoin 501 unverified-only) + public per-user/per-channel reads (core-donations, P14); vidra-user profile display + settings verification; backed `donations.spec.ts`. NON-CUSTODIAL only; premium/payouts DEFERRED | both |
| VIDRA-INFRA-HEALTH | devex | backend/api | architecture.md | Operators can probe liveness/readiness of the API and its dependencies | `/healthz` (liveness), `/readyz` (postgres+redis ping, 503 on degraded) | n/a (operator/orchestrator) | unit (httptest+fake pingers) PASS | VERIFIED | `cmd/api` + `internal/httpapi/health.go`; `componentHealth` shared with `GET /admin/system`; unit tests + `TestAPIStartupSmoke` (real binary polls `/readyz` until postgres+redis ok) green | vidra-core |
| VIDRA-INFRA-NODEINFO | devex/federation | backend/api | architecture.md | Public minimal instance-discovery metadata document | `GET /api/v1/nodeinfo` (name/software/instance); NodeInfo 2.1 at `/nodeinfo/2.1` when FEDERATION_ENABLED | discovery surfaces (later) | unit PASS; contract (later) | VERIFIED | `internal/httpapi/health.go` minimal nodeinfo + `internal/httpapi/federation.go` NodeInfo 2.1 + `/.well-known/nodeinfo` (gated on FEDERATION_ENABLED, incl. absent-when-disabled test); unit-tested | vidra-core |
| VIDRA-INFRA-MIGRATIONS | devex | backend/db | architecture.md, migrations/ | Fresh DB migrates cleanly to current schema | golang-migrate pairs; CI applies up-migrations on fresh Postgres | n/a | migration test in CI | VERIFIED | both backend CI workflows apply ALL migrations (0001–0067) to a fresh postgres:16 every run; `TestMigrationsAgainstExistingFixture` (`-tags=integration`) proves the chain against a POPULATED DB (fixture rows survive) — green on main 2026-07-03. DEFERRED: a systematic DOWN smoke (individual downs spot-verified) | vidra-core |
| PT-WATCH-STORYBOARD | peertube | backend/media/api | docs/use/watch (seek preview) | Seek-bar hover shows scrubbing thumbnails | ffmpeg tile sprite + WebVTT map generated on publish; `GET /videos/{id}/storyboard.{jpg,vtt}` visibility-gated; `has_storyboard` on detail | player storyboard overlay | unit (builders), service, handler | BACKEND_PARTIAL | core-media-2 2026-07-03: `internal/media/storyboard.go`, `video.Storyboarder` in Process, migration 0060 kinds, openapi; `make ci`+integration green. Frontend seek-preview overlay in vidra-user still pending (no backed spec yet) | vidra-core |
| PT-MEDIA-VP9 | peertube | backend/media | docs/admin/configuration (transcoding codecs) | Alternate VP9 rendition available for download | `TRANSCODING_VP9_ENABLED` → progressive VP9/WebM alternate at top rung; `/videos/{id}/webm` + `/download` kind=webm; AV1 deferred (config-rejected) | download-menu webm option | unit (args), transcode service, config | INTENTIONAL_DIFFERENCE | core-media-2 2026-07-03: `internal/media/vp9.go`, HLSTranscoder.SetVP9, transcode storeResult; a progressive WebM alternate (not an HLS variant); AV1 config-rejected/deferred; `make ci`+integration green | vidra-core |
| PT-ADMIN-MEDIA-GC | peertube | admin/backend/media | docs/admin (orphan cleanup) | Admin deletes storage blobs with no DB reference | `internal/mediagc` swept-prefix sweep (never touches unknown prefixes); `POST /admin/media/gc` dry-run default + daily worker; audited; `storage.ObjectLister` on Local+S3 | admin storage/GC panel | unit, storage unit, handler, MinIO integration | VERIFIED | core-media-2 2026-07-03: sqlc media_gc.sql, `make ci`+MinIO integration green; vidra-user admin storage/GC panel; e2e `admin-media.spec.ts` | vidra-core |
| VIDRA-PLAYLIST-COVER | vidra-extension | backend/frontend/api | user spec (studio playlist editor) | Owner uploads a playlist cover image | `playlists.thumbnail_ext` (migration 0060); POST/DELETE/GET `/playlists/{id}/thumbnail`; `has_thumbnail` on playlist views; GC-swept | studio cover upload/remove; card cover | service unit, handler | VERIFIED | core-media-2 2026-07-03: `internal/playlist` SetThumbnail/ClearThumbnail, openapi; `make ci` green; vidra-user studio playlist cover editor; covered by backed `playlists.spec.ts` | both |

## Rule

Ralph must add new rows as soon as a surveyed PeerTube control/API/workflow is discovered. Do not compress multiple distinct buttons or workflows into one vague row when they can fail independently.
