# Sprint 20: Release Hardening

Created: 2026-02-15
Status: PENDING
Approved: Yes
Iterations: 0
Worktree: No

> **Status Lifecycle:** PENDING → COMPLETE → VERIFIED
> **Iterations:** Tracks implement→verify cycles (incremented by verify phase)
>
> - PENDING: Initial state, awaiting implementation
> - COMPLETE: All tasks implemented
> - VERIFIED: All checks passed
>
> **Approval Gate:** Implementation CANNOT proceed until `Approved: Yes`
> **Worktree:** Set at plan creation (from dispatcher). `Yes` uses git worktree isolation; `No` works directly on current branch

## Summary

**Goal:** Complete the Quality Programme with final security validation, coverage sign-off, Atlas cleanup, release notes, maintenance plan, and Final Release Checklist verification. This is Sprint 20 (final sprint of 6).

**Architecture:** Documentation-focused with Makefile cleanup and security validation. No new production Go features. Removes legacy Atlas migration targets from Makefile. Creates CHANGELOG.md and finalizes maintenance plan.

**Tech Stack:** Go testing, gosec (via golangci-lint), govulncheck (install if needed), Makefile

## Scope

### In Scope

- Full regression suite execution (tests, lint, race detector, build)
- Security validation (gosec via golangci-lint, govulncheck for dependency vulnerabilities)
- Coverage threshold verification and final coverage report
- Remove Atlas migration targets from Makefile (atlas-install, atlas-version, related help text)
- Create comprehensive CHANGELOG.md / release notes (Sprints 1-20 summary)
- Finalize maintenance plan in QUALITY_PROGRAMME.md
- Work through Final Release Checklist checkboxes
- Sprint 20 completion documentation
- Update sprint status across docs (Sprint 19 → Sprint 20 complete)

### Out of Scope

- Staging/production deployment (no infrastructure access)
- Rollback rehearsal (requires staging environment)
- Release candidate Docker image build/push
- New production Go code features
- Federation handler coverage uplift (72.2% — complex crypto mocking, deferred post-programme)
- Integration test hermetic isolation (deferred post-programme)

## Prerequisites

- Sprint 19: VERIFIED (complete)
- All current tests pass (`go test -short ./...`)
- Git working tree clean

## Context for Implementer

> This section is critical for cross-session continuity.

- **Patterns to follow:**
  - Sprint completion docs follow format in `docs/sprints/SPRINT18_COMPLETE.md` and `SPRINT19_COMPLETE.md`
  - Coverage thresholds defined in `scripts/coverage-thresholds.txt`
  - Quality Programme acceptance criteria in `docs/sprints/QUALITY_PROGRAMME.md`
  - Makefile targets follow pattern: `.PHONY:` declaration, target with `##` help comment

- **Conventions:**
  - Sprint status tracked in: QUALITY_PROGRAMME.md, sprints/README.md, README.md, .claude/rules/project.md
  - Coverage numbers must cite actual `go test` output, never estimated
  - Release notes should be factual summaries, not marketing language

- **Key files the implementer must read first:**
  - `docs/sprints/QUALITY_PROGRAMME.md` — Sprint 20 acceptance criteria and Final Release Checklist
  - `Makefile` — Atlas targets to remove (lines ~625-640, ~735)
  - `scripts/coverage-thresholds.txt` — per-package coverage targets
  - `cmd/server/main.go` — version variable (line 23)

- **Gotchas:**
  - `govulncheck` is NOT installed — must install via `go install golang.org/x/vuln/cmd/govulncheck@latest`
  - gosec runs as part of `golangci-lint` (already configured in `.golangci.yml`)
  - The Makefile Atlas section (~lines 625-640) includes install commands and version checks — remove the entire block
  - Makefile help text (~line 735) references Atlas — update
  - `version = "dev"` in main.go — this is intentional (set via ldflags at build time)

- **Domain context:**
  - Quality Programme spans Sprints 15-20. Sprints 15-19 are COMPLETE/VERIFIED.
  - The Final Release Checklist in QUALITY_PROGRAMME.md is the authoritative sign-off document.
  - Some checklist items (staging deploy, monitoring alerts) cannot be verified locally — mark with notes explaining what can't be verified without infrastructure.

## Progress Tracking

**MANDATORY: Update this checklist as tasks complete. Change `[ ]` to `[x]`.**

- [ ] Task 1: Full regression and security validation
- [ ] Task 2: Coverage threshold verification and sign-off
- [ ] Task 3: Remove Atlas targets from Makefile
- [ ] Task 4: Create release notes (CHANGELOG.md)
- [ ] Task 5: Finalize maintenance plan
- [ ] Task 6: Complete Final Release Checklist
- [ ] Task 7: Sprint 20 completion documentation

**Total Tasks:** 7 | **Completed:** 0 | **Remaining:** 7

## Implementation Tasks

### Task 1: Full regression and security validation

**Objective:** Run full test suite, linter, race detector, and security scanners. Document results.

**Dependencies:** None

**Files:**

- Read: Test output, lint output, security scan output
- No files modified (validation only, unless issues found)

**Key Decisions / Notes:**

- Run these commands and capture output:
  1. `go test -short -count=1 ./...` — all tests pass
  2. `make lint` — golangci-lint (includes gosec) reports 0 issues
  3. `go test -race -short -count=1 ./...` — zero data races
  4. `make build` — binary builds successfully
  5. Install govulncheck: `go install golang.org/x/vuln/cmd/govulncheck@latest`
  6. `govulncheck ./...` — document any known vulnerabilities
- If govulncheck finds vulnerabilities: document them with severity. Critical/High vulns that have patches should be fixed (update dependency). Low/Medium vulns should be documented for maintenance plan.
- If any test failures or lint issues are found, fix them before proceeding.

**Definition of Done:**

- [ ] All tests pass (zero failures)
- [ ] Lint reports 0 issues
- [ ] Race detector finds 0 data races
- [ ] Build succeeds
- [ ] govulncheck results documented (any findings assessed and addressed or documented)

**Verify:**

- `go test -short -count=1 ./... 2>&1 | grep -c "FAIL"` — returns 0
- `make lint 2>&1 | tail -1` — shows "0 issues"
- `make build` — exit code 0

### Task 2: Coverage threshold verification and sign-off

**Objective:** Verify all per-package coverage thresholds are met. Generate final coverage report.

**Dependencies:** Task 1 (tests must pass first)

**Files:**

- Read: `scripts/coverage-thresholds.txt`
- Read: Coverage output from `go test -cover`
- No files modified unless thresholds need adjustment

**Key Decisions / Notes:**

- Run `make coverage-check` or `make coverage-per-package` to verify thresholds
- For each package in `scripts/coverage-thresholds.txt`, verify actual coverage meets or exceeds the threshold
- If any package is below threshold: investigate if the threshold was set too aggressively or if tests are missing. Adjust threshold if it was aspirational rather than achieved.
- Generate summary: packages meeting threshold, packages with headroom, overall average

**Definition of Done:**

- [ ] All per-package coverage thresholds verified against actual coverage
- [ ] Coverage results documented with actual vs threshold for each package
- [ ] Any threshold adjustments justified and documented

**Verify:**

- `make coverage-check` — passes (or coverage-per-package shows all above threshold)

### Task 3: Remove Atlas targets from Makefile

**Objective:** Remove all Atlas migration tool references from the Makefile. The project migrated to Goose — Atlas targets are dead code.

**Dependencies:** None

**Files:**

- Modify: `Makefile` — remove Atlas targets and references

**Key Decisions / Notes:**

- Remove the entire Atlas section (~lines 625-640): `atlas-install`, `atlas-version` targets and associated code
- Remove Atlas reference from help text (~line 735): `make atlas-version` line
- Keep all Goose targets intact (`migrate-up`, `migrate-down`, `migrate-status`, `migrate-create`, etc.)
- Verify no other Makefile targets depend on the Atlas targets before removing
- After removal, verify `make help` output is clean and `make build` still works

**Definition of Done:**

- [ ] No Atlas-related targets in Makefile (`grep -c "atlas" Makefile` returns 0 or only comments)
- [ ] `make help` output shows no Atlas references
- [ ] `make build` still succeeds after changes
- [ ] All Goose migration targets still work (`make migrate-status` if DB available, or verify target exists)

**Verify:**

- `grep -i "atlas" Makefile` — no results (or only in historical comments)
- `make help 2>&1 | grep -i atlas` — no results
- `make build` — succeeds

### Task 4: Create release notes (CHANGELOG.md)

**Objective:** Create comprehensive release notes documenting the entire development lifecycle from Sprint 1 through Sprint 20.

**Dependencies:** Tasks 1-2 (need final validation results to cite)

**Files:**

- Create: `CHANGELOG.md` (root level)
- Read: All `docs/sprints/SPRINT*_COMPLETE.md` files for sprint summaries
- Read: `docs/sprints/QUALITY_PROGRAMME.md` for programme overview

**Key Decisions / Notes:**

- Structure: Group by programme phase
  - **Feature Parity (Sprints 1-14):** One-line summary per sprint from completion docs
  - **Quality Programme (Sprints 15-20):** More detailed per-sprint entries
- Include sections: Features, Security Improvements, Infrastructure, Breaking Changes, Known Limitations
- Reference the version variable in `cmd/server/main.go` (currently `"dev"`, set via ldflags)
- Keep factual and concise — this is a technical changelog, not marketing material
- Note known limitations: federation handler coverage at 72.2%, whisper Docker image unpinned, integration test hermetic isolation deferred

**Definition of Done:**

- [ ] `CHANGELOG.md` exists at project root
- [ ] All 20 sprints represented with key achievements
- [ ] Security improvements section lists specific fixes
- [ ] Known limitations section is honest and complete
- [ ] File is well-organized and scannable

**Verify:**

- Read CHANGELOG.md and verify each sprint is represented
- Cross-reference with sprint completion docs for accuracy

### Task 5: Finalize maintenance plan

**Objective:** Expand the maintenance plan skeleton in QUALITY_PROGRAMME.md into a concrete, actionable post-release maintenance guide.

**Dependencies:** Tasks 1-2 (need validation results to inform plan)

**Files:**

- Modify: `docs/sprints/QUALITY_PROGRAMME.md` — expand "Maintenance Plan (Post-Release)" section

**Key Decisions / Notes:**

- The existing skeleton has: Monthly quality review, Security cadence, API governance, Style governance
- Expand each section with concrete actions, owners (role-based), and cadence
- Add sections for:
  - Dependency update schedule (monthly `go get -u`, quarterly major version review)
  - Coverage ratcheting policy (thresholds only go up, never down without justification)
  - Deferred work tracking (federation coverage, integration test isolation, test file renaming)
  - Incident response for test failures (flake detection, quarantine process)
- Keep actionable and realistic — avoid aspirational items that won't happen

**Definition of Done:**

- [ ] Maintenance Plan section has concrete actions with cadence for each area
- [ ] Deferred items from Sprints 17-19 documented with recommended priority
- [ ] Dependency update schedule defined
- [ ] Coverage ratcheting policy documented

**Verify:**

- Read the maintenance plan section; each action should be specific enough to execute

### Task 6: Complete Final Release Checklist

**Objective:** Work through each item in the Final Release Checklist in QUALITY_PROGRAMME.md. Mark items as verified or note what can't be verified locally.

**Dependencies:** Tasks 1-5 (need all validation complete)

**Files:**

- Modify: `docs/sprints/QUALITY_PROGRAMME.md` — update Final Release Checklist checkboxes

**Key Decisions / Notes:**

- Checklist categories:
  - **Mainline Integrity:** Verify no critical open PRs (use `gh pr list` if available)
  - **Security Baseline:** Verify via gosec results, check for secrets in config, injection protection
  - **API Contract:** Verify OpenAPI validates, types reproducible
  - **Testing and Coverage:** Verify coverage profiles, thresholds achieved, no flaky tests
  - **Documentation Accuracy:** Already done in Sprint 19
  - **Operational Readiness:** Cannot verify staging deploy/rollback locally — mark with note
- For each item: mark `[x]` if verified, or add note if cannot be verified locally
- Be honest about what can't be verified — don't mark staging items as complete

**Definition of Done:**

- [ ] Every checklist item either marked `[x]` with evidence or annotated with why it can't be verified
- [ ] All locally-verifiable items are verified
- [ ] Infrastructure-dependent items clearly noted

**Verify:**

- Read the Final Release Checklist; every item should have a status

### Task 7: Sprint 20 completion documentation

**Objective:** Create Sprint 20 completion report and update all status-tracking documents.

**Dependencies:** Tasks 1-6 (CRITICAL: all other tasks must be complete first)

**Files:**

- Create: `docs/sprints/SPRINT20_COMPLETE.md`
- Modify: `docs/sprints/QUALITY_PROGRAMME.md` — mark Sprint 20 acceptance criteria, update Last Updated date
- Modify: `docs/sprints/README.md` — add Sprint 20 entry
- Modify: `README.md` — update sprint status to "Quality Programme Complete"
- Modify: `.claude/rules/project.md` — update sprint status, Last Updated date

**Key Decisions / Notes:**

- SPRINT20_COMPLETE.md follows format of SPRINT18_COMPLETE.md and SPRINT19_COMPLETE.md
- Update sprint status across ALL status-tracking docs:
  - README.md: Sprint 20 as Complete, Quality Programme 100%
  - .claude/rules/project.md: Sprint status "Quality Programme Complete"
  - QUALITY_PROGRAMME.md: Sprint 20 criteria marked, Last Updated
  - sprints/README.md: Sprint 20 entry added
- Run `make update-readme-metrics` to capture final metrics
- This is the final sprint — the status should reflect programme completion

**Definition of Done:**

- [ ] `docs/sprints/SPRINT20_COMPLETE.md` exists with complete report
- [ ] Sprint 20 acceptance criteria marked in QUALITY_PROGRAMME.md
- [ ] Sprint status consistent across README.md, CLAUDE.md rules, QUALITY_PROGRAMME.md, sprints/README.md
- [ ] Quality Programme marked as 100% complete

**Verify:**

- Sprint status consistent across all docs: `grep -n "Sprint 20\|Quality Programme" README.md .claude/rules/project.md docs/sprints/QUALITY_PROGRAMME.md docs/sprints/README.md`

## Testing Strategy

- **Regression:** `go test -short -count=1 ./...` — full suite
- **Security:** `make lint` (gosec), `govulncheck ./...`
- **Race detection:** `go test -race -short -count=1 ./...`
- **Coverage:** `make coverage-per-package` against thresholds
- **Build:** `make build` — clean build
- **Makefile:** `make help` — no Atlas references after cleanup

## Risks and Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| govulncheck finds critical vulnerabilities requiring dependency updates | Medium | Medium | Assess severity; update if patch available, document if not |
| Coverage thresholds not met by some packages | Low | Low | Thresholds were ratcheted to achieved levels in Sprint 17-18; should pass |
| Makefile Atlas removal breaks other targets | Low | Medium | Verify no dependencies on Atlas targets before removal; test `make build` and `make help` after |
| Context exhaustion across 7 tasks | Medium | Medium | Natural session boundary after Tasks 1-3 (validation + cleanup), then Tasks 4-7 (docs). Each task is independent. |

## Open Questions

- None. Sprint 20 scope is defined by the Quality Programme and clarified through user questions.

### Deferred Ideas

- Federation handler coverage uplift (72.2% → 80%+) — complex crypto mocking, post-programme
- Integration test hermetic isolation — post-programme infrastructure work
- Test file renaming (_test.go → _unit_test.go consistency) — cosmetic, deferred from Sprint 19
- Release candidate Docker image build/push — requires CI/CD infrastructure
- Staging rollback rehearsal — requires staging environment
