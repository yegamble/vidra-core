# Sprint 19: Documentation Accuracy

Created: 2026-02-15
Status: VERIFIED
Approved: Yes
Iterations: 0
Worktree: No

> **Status Lifecycle:** PENDING → COMPLETE → VERIFIED
> **Iterations:** Tracks implement→verify cycles (incremented by verify phase)
>
> - PENDING: Initial state, awaiting implementation
> - COMPLETE: All tasks implemented
> - VERIFIED: All checks passed
>
> **Approval Gate:** Implementation CANNOT proceed until `Approved: Yes`
> **Worktree:** Set at plan creation (from dispatcher). `Yes` uses git worktree isolation; `No` works directly on current branch

## Summary

**Goal:** Ensure all living documentation reflects the actual implementation. Fix broken links, stale Atlas→Goose references, outdated metrics, and inaccurate runbook commands. Update all CLAUDE.md files with current patterns. Pin Docker images for hermetic builds. Run full race detector validation. Create comprehensive test infrastructure documentation and a source-of-truth map.

**Architecture:** Documentation-only changes to existing markdown files, docker-compose.yml image tag updates, and `go test -race` validation. No production Go code changes.

**Tech Stack:** Markdown, Docker Compose, Go testing (`-race` flag)

## Scope

### In Scope

- Fix all broken internal links in living documentation (README, CLAUDE.md, runbooks, development guides, deployment, operations, architecture, features, federation, security docs)
- Fix stale Atlas migration references in operational/active docs (runbooks, development guides)
- Update stale metrics in README.md (Go file count, test count, coverage %)
- Update stale metrics in docs/development/README.md (test count, coverage %)
- Update all 6 CLAUDE.md files with current patterns, Sprint 19 status, and accurate numbers
- Validate and fix runbook commands (docs/claude/runbooks.md, docs/operations/RUNBOOK.md)
- Pin Docker images in docker-compose.yml (replace `:latest` tags with specific versions)
- Run `go test -race -short` across all packages and fix any race conditions found
- Create comprehensive test infrastructure documentation
- Create source-of-truth navigation map documenting where canonical info lives
- Sprint 19 completion documentation

### Out of Scope

- Historical sprint completion docs (SPRINT1_COMPLETE.md through SPRINT14_COMPLETE.md) — these are point-in-time records
- Historical progress/plan files (_PROGRESS.md,_PLAN.md, _EXECUTION_PLAN.md)
- Production Go code changes (exception: race condition fixes if found)
- docs/reports/ directory — one-off analysis reports, not living documentation
- docs/project-management/ — historical planning artifacts
- Test file renaming (_test.go vs_unit_test.go) — cosmetic, low priority, deferred to Sprint 20
- Staging environment runbook validation (no staging access)

## Prerequisites

- All current tests pass (`go test -short ./...`)
- Sprint 18: VERIFIED (complete)
- Git working tree clean (or at least no conflicts with docs changes)

## Context for Implementer

> This section is critical for cross-session continuity. Write it for an implementer who has never seen the codebase.

- **Patterns to follow:**
  - README.md has a `make update-readme-metrics` target and a `make check-readme-metrics` target for automated metric updates. Use these to verify metrics after changes.
  - CLAUDE.md files serve as module-level documentation for Claude. They describe architecture, patterns, commands, and conventions. Root `CLAUDE.md` is read by Claude as project instructions.
  - Runbooks (`docs/claude/runbooks.md`, `docs/operations/RUNBOOK.md`) contain operational commands. Validate each command against the actual Makefile targets and docker-compose.yml services.

- **Conventions:**
  - Markdown links use relative paths: `[Text](../path/to/file.md)`
  - Sprint status is tracked in QUALITY_PROGRAMME.md, sprints/README.md, and root README.md — keep all three in sync
  - Coverage numbers come from `go test -short -cover ./...` — always cite actual output, never estimate

- **Key files the implementer must read first:**
  - `README.md` — main project README with metrics table
  - `CLAUDE.md` — root Claude instructions (sprint status line)
  - `docs/claude/runbooks.md` — operational runbook (has Atlas references that need Goose)
  - `docs/operations/RUNBOOK.md` — operations runbook
  - `docs/development/README.md` — development docs index (stale metrics)
  - `docs/sprints/QUALITY_PROGRAMME.md` — Sprint 19 acceptance criteria
  - `docker-compose.yml` — Docker image tags to pin

- **Gotchas:**
  - The runbook at `docs/claude/runbooks.md` references `atlas migrate` commands — the project migrated to Goose. The correct commands are in `migrations/CLAUDE.md` and the Makefile `migrate-*` targets.
  - `docs/development/README.md` claims 156 test files and 85%+ coverage — actual is 307 test files and 54.1% coverage.
  - README.md metrics (Go files: 516, test files: 232) are outdated — actual counts are 619 Go files, 313 test files.
  - Some docs reference `docker-compose.prod.yml` and `docker-compose.test.yml` which don't exist — only `docker-compose.yml` exists.
  - 24+ docs reference Atlas, but only operational docs need fixing. Historical sprint docs that mention Atlas are point-in-time records and should be left as-is.
  - docker-compose.yml has multiple service profiles (test, integration, etc.) — pin images in ALL profiles.

- **Living vs Historical doc classification (when uncertain):**
  1. Is it linked from README.md, CLAUDE.md, or any `docs/*/README.md`? → **Living** (fix it)
  2. Is it in `docs/sprints/` and ends with `_COMPLETE.md`, `_PROGRESS.md`, or `_PLAN.md`? → **Historical** (skip)
  3. Is it in `docs/project-management/`? → **Historical** (skip, unless it defines a current process)
  4. Is it in `docs/reports/`? → **Historical** (skip, one-off analysis reports)
  5. Does it document a completed migration/fix from before 2026-02-01? → **Historical** (skip)
  6. Examples: `docs/sprints/QUALITY_PROGRAMME.md` = Living (active acceptance criteria); `docs/sprints/SPRINT18_COMPLETE.md` = Historical; `docs/development/TEST_BASELINE_REPORT.md` = Historical (snapshot referenced for context but not actively maintained)

- **Domain context:**
  - The project migrated from Atlas to Goose for database migrations. `docs/MIGRATION_TO_GOOSE.md` documents this historical migration. The migration is complete — Goose is the only tool in use.
  - The project has 181 markdown files in docs/. Only "living" docs (actively referenced guides, runbooks, READMEs) are in scope. Historical sprint reports are excluded.

## Broken Links Found (Exploration Phase)

These files are linked to from docs but DO NOT exist:

| Missing File | Linked From |
|---|---|
| `infrastructure/README.md` | Various docs |
| `SECURITY.md` (root) | Various docs |
| `docs/api/README.md` | Feature/deployment docs |
| `docs/ci-fixes/go-module-proxy-dns-fix.md` | CI docs |
| `docs/FEDERATION_AUDIT_REPORT.md` | Federation docs |
| `docs/helm/README.md` | Deployment docs |
| `docs/PRODUCTION.md` | Deployment docs |
| `docs/SECURITY_E2EE.md` | Security docs |

## Atlas References in Living Docs

These living/operational docs still reference Atlas instead of Goose:

| Doc | Type | Fix Needed |
|---|---|---|
| `docs/claude/runbooks.md` | Operational runbook | Replace atlas commands with goose/make commands |
| `docs/development/README.md` | Development guide | Update migration section |
| `docs/development/CODE_QUALITY_REVIEW.md` | Quality guide | Update migration references |
| `docs/development/TEST_BASELINE_REPORT.md` | Test baseline | Update migration tool name |
| `docs/security/SECURITY_FIX_CHECKLIST.md` | Security checklist | Update migration references |
| `docs/claude/contributing.md` | Contributing guide | Update migration commands |

## Stale Metrics Found (Exploration Phase)

| Doc | Metric | Claims | Actual |
|---|---|---|---|
| `README.md` | Go Files | 516 | 619 |
| `README.md` | Test Files | 232 | 313 |
| `README.md` | Lines of Code | 185,654 | Need re-count |
| `docs/development/README.md` | Total Test Files | 156 | 307 |
| `docs/development/README.md` | Code Coverage | 85%+ | 54.1% |

## Docker Images to Pin

| Service | Current Tag | Action |
|---|---|---|
| `ipfs/kubo` | `:latest` (2 instances) | Pin to specific version |
| `onerahmet/openai-whisper-asr-webservice` | `:latest-gpu` | Pin to specific version |
| `clamav/clamav` | `:latest` (1 instance) | Pin to specific version (note: another instance already uses `:stable`) |

## Progress Tracking

**MANDATORY: Update this checklist as tasks complete. Change `[ ]` to `[x]`.**

- [x] Task 1: Fix broken links across living documentation
- [x] Task 2: Fix Atlas→Goose references in operational docs
- [x] Task 3: Update stale metrics in README and development docs
- [x] Task 4: Validate and fix runbook commands
- [x] Task 5: Update all CLAUDE.md files
- [x] Task 6: Pin Docker images in docker-compose.yml
- [x] Task 7: Race detector validation across full codebase
- [x] Task 8: Create test infrastructure documentation
- [x] Task 9: Create source-of-truth map
- [x] Task 10: Sprint 19 completion documentation

**Total Tasks:** 10 | **Completed:** 10 | **Remaining:** 0

## Implementation Tasks

### Task 1: Fix broken links across living documentation

**Objective:** Find all broken internal links in living documentation and either fix the link target, redirect to the correct file, or remove the dead link.

**Dependencies:** None

**Files:**

- Modify: All living docs that contain broken links (identified during exploration)
- Read: Verify which link targets actually exist before fixing

**Key Decisions / Notes:**

- For each broken link, determine: (a) Does the content exist elsewhere under a different name? If so, update the link. (b) Was the file removed intentionally? If so, remove the link. (c) Is the link to planned-but-not-created content? Add a note or remove.
- Run a comprehensive broken link scan covering all internal link types:
  - Markdown links to .md files: `grep -roh '\[.*\]([^)]*\.md)' docs/`
  - Links to other file types (.yaml, .yml, .sql, .txt): `grep -roh '\[.*\]([^)]*\.\(yaml\|yml\|sql\|txt\))' docs/`
  - Then verify each target exists relative to the directory containing the referencing file.
  - External HTTP links are out of scope for this task (would require network access to verify).
- Focus on docs/ directory and root-level docs (README, CLAUDE.md, CONTRIBUTING.md).
- Living docs include: README files (any README.md), CLAUDE.md files, docs/deployment/*, docs/operations/*, docs/development/*, docs/features/*, docs/federation/*, docs/security/*, docs/architecture/*, docs/claude/*.

**Definition of Done:**

- [ ] No broken internal links in any living documentation file
- [ ] Dead links either updated to correct target or removed with appropriate replacement text
- [ ] Verify by running link scan again: `grep -roh '\[.*\]([^)]*\.md)' docs/ | sed 's/.*(\([^)]*\))/\1/' | sort -u` and checking each target exists

**Verify:**

- `grep -roh '\[.*\]([^)]*\.md)' docs/ | sed 's/.*(\([^)]*\))/\1/' | sort -u` — extract all link targets, verify each exists relative to its source file

### Task 2: Fix Atlas→Goose references in operational docs

**Objective:** Replace all Atlas migration references with correct Goose commands in living/operational documentation.

**Dependencies:** None

**Files:**

- Modify: `docs/claude/runbooks.md` — replace atlas migrate commands with goose/make commands
- Modify: `docs/development/README.md` — update migration section
- Modify: `docs/development/CODE_QUALITY_REVIEW.md` — update migration references
- Modify: `docs/development/TEST_BASELINE_REPORT.md` — update migration tool name
- Modify: `docs/security/SECURITY_FIX_CHECKLIST.md` — update migration references
- Modify: `docs/claude/contributing.md` — update migration commands
- Read: `migrations/CLAUDE.md` — canonical Goose command reference
- Read: `Makefile` — actual make targets for migrations

**Key Decisions / Notes:**

- The correct migration commands come from `migrations/CLAUDE.md` and the Makefile:
  - `make migrate-up` (apply), `make migrate-down` (rollback), `make migrate-status` (status), `make migrate-create NAME=...` (new migration)
  - Direct goose: `goose -dir migrations postgres "$DATABASE_URL" up/down/status`
- The `docs/claude/runbooks.md` has an entire "Run Migrations" section using Atlas commands — replace entirely with Goose equivalents
- Historical docs (sprint completion reports) that mention Atlas should NOT be changed — they're accurate for when they were written
- The `docs/MIGRATION_TO_GOOSE.md` doc is itself historical — describes the migration from Atlas to Goose. Leave as-is.

**Definition of Done:**

- [ ] No Atlas migration commands in any living/operational documentation
- [ ] All migration commands match actual Makefile targets or direct goose CLI syntax
- [ ] Verify: `grep -ri "atlas migrate\|atlas schema" docs/claude/ docs/development/ docs/security/ docs/operations/ docs/deployment/ docs/features/ docs/federation/` returns no results
- [ ] No Atlas migration tool references in Go code comments or migration SQL comments: `grep -ri "atlas" internal/ migrations/ --include="*.go" --include="*.sql"` — inspect results and update/remove stale Atlas references in comments

**Verify:**

- `grep -ri "atlas migrate\|atlas schema" docs/claude/ docs/development/ docs/security/ docs/operations/ docs/deployment/` — should return no results in living docs
- `grep -ri "atlas" internal/ migrations/ --include="*.go" --include="*.sql"` — inspect results, any Atlas migration tool references in comments should be updated

### Task 3: Update stale metrics in README and development docs

**Objective:** Update all stale metrics (file counts, test counts, coverage percentages, lines of code) to reflect the actual current state.

**Dependencies:** None

**Files:**

- Modify: `README.md` — update Project Metrics table
- Modify: `docs/development/README.md` — update Test Coverage Metrics section
- Read: Run commands to get actual metrics

**Key Decisions / Notes:**

- First, verify `make update-readme-metrics` runs successfully. If it fails (missing deps, wrong syntax), switch to manual metric collection using the commands below. The Makefile has `update-readme-metrics` and `check-readme-metrics` targets (verified during exploration). Manual fallback commands:
  - Go files: `find . -name "*.go" ! -path "*/vendor/*" | wc -l`
  - Test files: `find . -name "*_test.go" ! -path "*/vendor/*" | wc -l`
  - Source files: total - test files
  - Lines of code: `find . -name "*.go" ! -path "*/vendor/*" | xargs wc -l | tail -1`
  - Test count: `grep -r "func Test" internal/ --include="*_test.go" | wc -l`
  - Coverage: use existing `go test -short -cover` output
  - Migration count: `ls migrations/*.sql | wc -l`
- The development README metrics section is badly outdated (156 test files, 85%+ coverage). Fix with actual numbers.
- Run `make check-readme-metrics` after to verify.

**Definition of Done:**

- [ ] README.md Project Metrics table matches actual file/test/coverage counts
- [ ] `docs/development/README.md` Test Coverage Metrics section matches actual counts
- [ ] `make check-readme-metrics` passes (if available)
- [ ] No metrics differ from actual values by more than ±1

**Verify:**

- `make check-readme-metrics` — passes or shows no drift
- Manual verification: `find . -name "*.go" ! -path "*/vendor/*" | wc -l` matches README count

### Task 4: Validate and fix runbook commands

**Objective:** Ensure all commands in operational runbooks are correct and work with the current infrastructure.

**Dependencies:** Task 2 (Atlas references fixed first)

**Files:**

- Modify: `docs/claude/runbooks.md` — fix any incorrect commands
- Modify: `docs/operations/RUNBOOK.md` — validate and fix commands
- Modify: `docs/operations/MONITORING.md` — validate monitoring commands
- Modify: `docs/operations/PERFORMANCE.md` — validate performance commands
- Read: `docker-compose.yml` — verify service names, ports, profiles
- Read: `Makefile` — verify make targets exist

**Key Decisions / Notes:**

- For each command in runbooks, verify:
  1. The binary/tool referenced exists (goose not atlas, docker compose not docker-compose)
  2. The service names match docker-compose.yml
  3. The ports match actual configuration
  4. The make targets exist in the Makefile
  5. The docker-compose file references are correct (no `docker-compose.prod.yml` references)
- `docker-compose.prod.yml` does NOT exist — any references need to be removed or updated to the correct approach
- Check health endpoint paths match the actual routes in the codebase
- The `docs/claude/runbooks.md` references `docker compose exec postgres psql -U vidra_user -d vidra` — verify these credentials match docker-compose.yml

**Definition of Done:**

- [ ] All commands in `docs/claude/runbooks.md` reference correct tools, services, and ports
- [ ] All commands in `docs/operations/RUNBOOK.md` reference correct tools, services, and ports
- [ ] No references to `docker-compose.prod.yml` or `docker-compose.test.yml` in operational docs
- [ ] All make targets referenced actually exist in Makefile
- [ ] Migration commands use Goose syntax (verified by Task 2)
- [ ] Critical commands tested against local dev stack where possible: `make build`, `make test`, `make lint`, `make migrate-status` — document output. Commands requiring production infra (Docker services, health checks) verified for syntax correctness only.

**Verify:**

- Cross-reference each runbook command category against: `docker-compose.yml` (services, ports), `Makefile` (targets), codebase (endpoints, paths)
- Run: `make build`, `make test-unit`, `make lint` — verify these execute successfully

### Task 5: Update all CLAUDE.md files

**Objective:** Ensure all 6 CLAUDE.md files accurately reflect the current state of their respective modules, with current sprint status, coverage numbers, and patterns.

**Dependencies:** Task 3 (metrics updated first)

**Files:**

- Modify: `CLAUDE.md` (root) — update sprint status to "Sprint 19/20", verify test counts and coverage
- Modify: `docs/architecture/CLAUDE.md` — verify architecture descriptions match current code
- Modify: `internal/httpapi/CLAUDE.md` — verify handler patterns, middleware stack
- Modify: `internal/security/CLAUDE.md` — verify security tooling descriptions
- Modify: `internal/activitypub/CLAUDE.md` — verify federation endpoints and patterns
- Modify: `migrations/CLAUDE.md` — verify Goose commands are current
- Read: Actual code to verify patterns described in CLAUDE.md files

**Key Decisions / Notes:**

- Root CLAUDE.md: update "Sprint 18/20" → "Sprint 19/20", update test count (3,715 → actual), update coverage (52.9% → actual)
- For each module CLAUDE.md, spot-check 2-3 code references to ensure they still match the actual code. For example, verify the handler pattern in httpapi/CLAUDE.md matches what handlers actually look like.
- Don't rewrite CLAUDE.md files — make targeted corrections where information has drifted.
- Add any new patterns discovered during Sprint 18 (e.g., interface extraction for testability, new test file naming convention `_unit_test.go`).
- **Verification method:** For each CLAUDE.md, grep for 2-3 function signatures or patterns mentioned in the doc. If the doc says handlers use pattern X, find 3 actual handlers and verify they match. If examples are outdated, update CLAUDE.md with current examples from the codebase.

**Definition of Done:**

- [ ] Root `CLAUDE.md` has correct sprint status, test count, and coverage percentage
- [ ] Each module CLAUDE.md has been reviewed and corrected where needed
- [ ] No CLAUDE.md file references tools, patterns, or files that no longer exist
- [ ] New patterns from Sprint 18 (interface extraction for handler testability) are documented

**Verify:**

- Read each CLAUDE.md and verify: sprint status consistent, coverage numbers match `go test` output, referenced files exist

### Task 6: Pin Docker images in docker-compose.yml

**Objective:** Replace `:latest` Docker image tags with pinned version tags for hermetic, reproducible builds.

**Dependencies:** None

**Files:**

- Modify: `docker-compose.yml` — pin all `:latest` tags to specific versions
- Read: Docker Hub pages for current stable versions

**Key Decisions / Notes:**

- Images to pin:
  - `ipfs/kubo:latest` → Check <https://hub.docker.com/r/ipfs/kubo/tags> for latest stable release tag, pin to it (e.g., `ipfs/kubo:v0.33.0`)
  - `onerahmet/openai-whisper-asr-webservice:latest-gpu` → Check Docker Hub tags page, pin to most recent versioned GPU tag
  - `clamav/clamav:latest` → Use `:stable` tag (one instance already uses `:stable`, make consistent)
- For each image: verify the chosen tag exists with `docker pull <image>:<tag>` (or check Docker Hub web if offline). Document the chosen versions in task completion notes.
- **Fallback versions if Docker Hub unavailable:** `ipfs/kubo:v0.32.1`, `clamav/clamav:stable`. For `onerahmet/openai-whisper-asr-webservice`, check if versioned tags exist — this image may only use `:latest-gpu`; if so, document the limitation and keep current tag with a note.
- Already-pinned images (`postgres:15-alpine`, `redis:7-alpine`) are fine.
- After pinning, verify `docker compose config` parses successfully.
- Don't change the application image (`vidra:latest`) — that's built locally and intentionally uses latest.

**Definition of Done:**

- [ ] No `:latest` tags on external images in docker-compose.yml (except the local `vidra:latest` build)
- [ ] All pinned versions are current stable releases
- [ ] `docker compose config` runs without errors

**Verify:**

- `grep ":latest" docker-compose.yml` — only shows `vidra:latest` (the local build)
- `docker compose config > /dev/null 2>&1 && echo "OK"` — no parse errors

### Task 7: Race detector validation across full codebase

**Objective:** Run `go test -race -short` across the entire codebase and fix any race conditions found.

**Dependencies:** None

**Files:**

- Read: Any packages that fail with race conditions
- Modify: Any Go files with race conditions (if found)

**Key Decisions / Notes:**

- Run in stages to identify specific failing packages:
  1. `go test -race -short -count=1 ./internal/domain/...` (already verified clean)
  2. `go test -race -short -count=1 ./internal/usecase/...` (already verified clean)
  3. `go test -race -short -count=1 ./internal/repository/...`
  4. `go test -race -short -count=1 ./internal/httpapi/...`
  5. `go test -race -short -count=1 ./internal/middleware/...`
  6. `go test -race -short -count=1 ./internal/security/...`
  7. `go test -race -short -count=1 ./internal/worker/...`
  8. `go test -race -short -count=1 ./internal/storage/...`
  9. `go test -race -short -count=1 ./internal/activitypub/...`
  10. Remaining packages
- If races found: fix with proper synchronization (mutex, atomic, channel). Follow existing patterns in the codebase. **Only fix races that require isolated changes (add mutex, use atomic, fix channel usage).** If a race requires architectural changes (goroutine redesign, worker pool refactoring), document the race details, create a clear issue description for Sprint 20, and add a known issue note to the completion report.
- If no races found: document the clean result in Sprint 19 completion report.
- This was deferred from Sprint 17 — completing it closes that open item.

**Definition of Done:**

- [ ] `go test -race -short -count=1 ./...` passes with 0 race conditions
- [ ] Any race fixes include regression tests
- [ ] Results documented (clean or fixed)

**Verify:**

- `go test -race -short -count=1 ./...` — all packages pass, no DATA RACE output

### Task 8: Create test infrastructure documentation

**Objective:** Create a comprehensive test infrastructure guide documenting test patterns, setup requirements, local development fast paths, and CI test configuration.

**Dependencies:** Task 7 (race detector results inform this doc)

**Files:**

- Create: `docs/development/TEST_INFRASTRUCTURE.md`
- Read: Existing test files for pattern examples, `Makefile` for test targets, `docker-compose.yml` for test services

**Key Decisions / Notes:**

- Document:
  - Test categories: unit (`*_test.go`, `*_unit_test.go`), integration (`*_integration_test.go`), e2e (`tests/e2e/`)
  - Test infrastructure requirements: PostgreSQL, Redis, IPFS, ClamAV (when needed)
  - How to run tests: `make test-unit` (fast), `make test` (all short), `make test-integration` (needs Docker)
  - Test patterns: table-driven tests, sqlmock for repos, httptest for handlers, testutil helpers
  - Coverage: `make coverage-check`, `make coverage-per-package`, thresholds in `scripts/coverage-thresholds.txt`
  - CI configuration: what runs in CI, how to reproduce CI locally
  - Local fast paths: `go test -short` skips integration tests, `go test -run TestName` for specific tests
  - Race detector: `go test -race -short` (now validated clean as of Sprint 19)
- This was deferred from Sprint 18 as "Document test infrastructure and local dev fast paths."
- Keep under 200 lines — focused reference, not a textbook.
- **Document ACTUAL categorization, not ideal.** The Makefile `test-unit` target excludes the repository package (repository tests need DB). If categorization is inconsistent (some integration tests use `_test.go` instead of `_integration_test.go`), note this in the doc and reference the deferred Sprint 20 cleanup task for test file renaming.

**Definition of Done:**

- [ ] `docs/development/TEST_INFRASTRUCTURE.md` exists at the specified path
- [ ] All test categories documented (unit, integration, e2e) with examples from codebase
- [ ] All `make test*` targets documented and verified against Makefile
- [ ] Test patterns section includes table-driven test and sqlmock examples
- [ ] Coverage section documents `make coverage-check`, `make coverage-per-package`, and thresholds file
- [ ] CI configuration section explains what runs in CI
- [ ] Local fast paths section documents `-short` and `-run` flags
- [ ] Race detector section references Sprint 19 validation results
- [ ] File is under 200 lines (focused reference)
- [ ] `docs/development/README.md` updated to link to new file

**Verify:**

- Read the new file; verify each command/target against Makefile and codebase
- `wc -l docs/development/TEST_INFRASTRUCTURE.md` — under 200 lines

### Task 9: Create source-of-truth map

**Objective:** Create a documentation index that maps each topic area to its canonical source document, eliminating ambiguity about where to find authoritative information.

**Dependencies:** Tasks 1-5 (links and content fixed first)

**Files:**

- Create: `docs/DOCUMENTATION_MAP.md`
- Modify: `docs/development/README.md` — add link to map
- Read: All living documentation to classify

**Key Decisions / Notes:**

- Map structure:

  ```
  | Topic | Canonical Source | Also Referenced In |
  |-------|-----------------|-------------------|
  | Project overview | README.md | CLAUDE.md |
  | Architecture | docs/architecture/CLAUDE.md | - |
  | Database migrations | migrations/CLAUDE.md | docs/claude/runbooks.md |
  | ...etc |
  ```

- Create one row per major topic area: project overview, architecture, API/HTTP layer, security, federation, migrations, deployment, Docker setup, testing, CI/CD, monitoring, operations, sprint status. Aim for 12-15 rows.
- For each topic, identify ONE canonical source and list where the topic is also mentioned. Example: "Database migrations | migrations/CLAUDE.md | docs/claude/runbooks.md (operational commands), docs/development/README.md (setup section)"
- This directly addresses the Sprint 19 acceptance criterion: "Single source of truth map created."
- Keep it concise — a reference table, not prose.
- **If multiple docs cover same topic:** Designate the most comprehensive/actively maintained as canonical. Add notes to other docs redirecting to canonical source. Do NOT attempt to merge or deduplicate in this sprint — note conflicts in the map under "Also Referenced In" column with status (e.g., "partially overlaps, consider consolidation in Sprint 20").

**Definition of Done:**

- [ ] `docs/DOCUMENTATION_MAP.md` exists with canonical source for every major topic area
- [ ] No topic has conflicting canonical sources
- [ ] Cross-references are accurate (linked files exist)

**Verify:**

- Read the map; verify each linked file exists and actually covers the listed topic

### Task 10: Sprint 19 completion documentation

**Objective:** Create Sprint 19 completion report and update all related documentation to reflect Sprint 19 status.

**Dependencies:** Tasks 1-9 (CRITICAL: all other tasks must be complete first)

**Files:**

- Create: `docs/sprints/SPRINT19_COMPLETE.md`
- Modify: `docs/sprints/QUALITY_PROGRAMME.md` — mark Sprint 19 acceptance criteria
- Modify: `docs/sprints/README.md` — add Sprint 19 entry
- Modify: `README.md` — update sprint status if not already done

**Key Decisions / Notes:**

- SPRINT19_COMPLETE.md should follow the format of SPRINT18_COMPLETE.md: achievements, files changed, acceptance criteria, statistics
- Mark Sprint 19 acceptance criteria in QUALITY_PROGRAMME.md with `[x]`:
  - No broken links in documentation
  - All docs validated against implementation
  - Runbooks tested and confirmed working
  - Single "source of truth" map created
- Add Sprint 19 entry to sprints README.md under Quality Programme section
- Update root README.md sprint status table

**Definition of Done:**

- [ ] `docs/sprints/SPRINT19_COMPLETE.md` exists with complete report
- [ ] `docs/sprints/QUALITY_PROGRAMME.md` has Sprint 19 criteria marked `[x]`
- [ ] `docs/sprints/README.md` includes Sprint 19 entry with link
- [ ] Sprint status consistent across README.md, CLAUDE.md, and QUALITY_PROGRAMME.md

**Verify:**

- Cross-reference sprint status across all modified files — must be consistent
- All Sprint 19 acceptance criteria verifiable against actual doc state

## Testing Strategy

- **Link validation:** Grep-based broken link scanning across all docs
- **Metrics validation:** Compare doc claims against actual `go test`, `find`, `wc` output
- **Command validation:** Cross-reference runbook commands against Makefile targets and docker-compose.yml
- **Race detection:** `go test -race -short -count=1 ./...` across full codebase
- **Docker validation:** `docker compose config` parses successfully after image pinning
- **Manual verification:** Read each modified doc and verify accuracy against codebase

## Risks and Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Broken link fixes cascade — fixing one link reveals more broken links | Medium | Low | Scan comprehensively in Task 1 before fixing; re-scan after fixes |
| Race detector finds deep concurrency bugs requiring complex fixes | Low | High | If a race fix requires significant code changes, document it and defer the fix to Sprint 20 with a clear issue description |
| Docker image version lookup fails (no internet or Docker Hub down) | Low | Medium | Use `docker pull` to verify tags exist; if unavailable, use known recent stable versions from Docker Hub web |
| Metrics drift between task completion and Sprint 19 documentation | Low | Low | Run metrics commands fresh during Task 10, not earlier |
| Context exhaustion across 10 tasks in multiple sessions | Medium | Medium | Natural session boundaries: after Tasks 1-3 (doc fixes), after Task 7 (race detector). Each session starts by reading ONLY the specific task being resumed. Use continuation file to track intra-task state (e.g., "Task 1: fixed 5/8 broken links, remaining: [list]"). |

## Open Questions

- None. Sprint 19 scope is well-defined in the Quality Programme and clarified through user questions.

### Deferred Ideas

- Test file renaming (`_test.go` → `_unit_test.go` consistency) — cosmetic, defer to Sprint 20
- Historical sprint doc updates — leave as point-in-time records
- docs/reports/ directory cleanup — one-off reports, not living documentation
