# golangci-lint v1.50+ uses schema version v1
# Config Reference: https://golangci-lint.run/usage/configuration/
run:
  tests: false
linters-settings:
  gocyclo:
    min-complexity: 15
  dupl:
    threshold: 150
  gosec:
    severity: medium
linters:
  enable:
    - bodyclose
    - dupl
    - gocyclo
    - gosec
    - misspell
    - nolintlint
    - rowserrcheck
    - sqlclosecheck
    - unconvert
    - whitespace
issues:
  max-issues-per-linter: 50
  max-same-issues: 10
